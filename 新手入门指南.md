# 智教慧学 — 新手入门指南

> 本文档以 `e:\PythonProject\chuma` 当前仓库代码为准，更新时间为 **2026-08-01**。\
> 目标：让接手者知道这个项目现在到底用了什么技术、这些技术在运行时怎样配合、每个文件的作用是什么、以及项目目前真实做到什么程度。

***

# 安装环境与配置检查、启动

## 前置环境检查

检查 Node

```powershell
node -v
npm -v

# 能输出版本号即可
```

检查 Python

```powershell
python --version
```

检查 Docker

```powershell
docker --version
docker compose version
```

检查项目目录

```powershell
cd e:\PythonProject\chuma
Get-ChildItem
```

检查虚拟环境是否存在

```powershell
Test-Path .\backend\venv\Scripts\python.exe
Test-Path .\ai\venv\Scripts\python.exe

# 预期：最好都返回 True
# 如果返回 False，说明你需要自己创建虚拟环境
```

## 环境变量配置

项目根目录需要 `.env` 文件（从 `.env.example` 复制并填写实际值）。关键配置项包括：

- `DATABASE_URL` — PostgreSQL 连接串
- `REDIS_URL` — Redis 连接串
- `JWT_SECRET_KEY` — JWT 签名密钥
- `AI_SERVICE_URL` — AI 服务地址（如 `http://localhost:8009`）
- `AI_SERVICE_TOKEN` / `SERVICE_TOKEN` — 服务间认证 token（两者必须一致）
- `AGE_HOST` / `AGE_PORT` / `AGE_DB` / `AGE_USER` / `AGE_PASSWORD` — Apache AGE 图数据库连接
- `LOCAL_MODEL_URL` / `LOCAL_MODEL_NAME` — 本地模型地址（vLLM）
- `REMOTE_MODEL_API_KEY` / `REMOTE_MODEL_BASE_URL` / `REMOTE_MODEL_NAME` — 远程大模型

## 启动中间件（PostgreSQL + Redis + ChromaDB）

```powershell
cd e:\PythonProject\chuma
docker compose -f docker-compose.middleware.yml up -d

# 验证
docker ps
```

> **注意**：知识图谱功能还需要 Apache AGE 扩展。如果 PostgreSQL 未安装 AGE，图谱构建链路将无法运行。详见第六节。

## 安装 AI 服务依赖并启动

如果存在虚拟环境，在虚拟环境中安装 AI 依赖：

```powershell
cd e:\PythonProject\chuma\ai
.\venv\Scripts\python.exe -m pip install -r requirements.txt

# 请注意：如果在终端里直接敲下面这两行（并且没有先执行激活虚拟环境的 Activate.ps1），
# 那么它就会根据你系统环境变量里配置的 pip 或 python，把包装进你的全局 Python 环境里。
# pip install -r requirements.txt        # 不推荐
# python -m pip install -r requirements.txt  # 不推荐
```

启动 AI 服务（**本地开发端口 8009**，远程服务器端口 8001）：

```powershell
cd e:\PythonProject\chuma\ai
# 方式一：激活虚拟环境后启动
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --port 8009

# 方式二：直接启动（不激活虚拟环境）
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8009

# 验证 AI 服务是否启动（新开一个终端），预期返回：{"status":"ok"}
curl http://localhost:8009/health

# 检查 AI 文档页（浏览器打开）
http://localhost:8009/docs

# 检查 AI 端口
netstat -ano | findstr :8009
```

> **端口说明**：本地开发 AI 服务默认使用 **8009** 端口，远程服务器上使用 **8001** 端口（见 CLAUDE.md）。两个端口口径对应的是同一份代码，只是不同部署环境。

## 启动 MCP 数据库服务（可选，Agent 功能依赖）

MCP（Model Context Protocol）数据库服务为 AI Agent 提供结构化的数据库查询能力，让大模型可以通过标准工具接口访问 PostgreSQL 中的业务数据。

```powershell
cd e:\PythonProject\chuma\scripts
# 使用 ai 虚拟环境或全局环境（确保安装了 mcp, fastmcp, asyncpg）
..\ai\venv\Scripts\python.exe mcp_db_server.py

# 验证 MCP 服务是否启动
netstat -ano | findstr :8005
```

## 安装 Backend 依赖并启动

```powershell
cd e:\PythonProject\chuma\backend
.\venv\Scripts\python.exe -m pip install -r requirements.txt
```

先执行数据库迁移（确保数据库连通性正常）：

```powershell
cd e:\PythonProject\chuma\backend
.\venv\Scripts\python.exe -m alembic upgrade head

# 如果迁移成功，说明数据库连通性至少基本没问题
```

> 当前已有 3 个迁移文件：
> - `001_add_kg_graphs_table.py` — 知识图谱元数据表
> - `002_student_side_tables.py` — 学生/教师/课程/题目/做题记录/掌握度表
> - `003_add_document_chunks.py` — 文档切片向量存储表（pgvector）
>
> 另外，以下增量 SQL 脚本通过 `docs/sql/` 目录执行，而非 Alembic 迁移：
> - `docs/sql/002_alter_tables.sql` — 为 `kg_graphs` 表新增 `course_id` 列（学科 → 知识图谱 映射）
> - `docs/sql/003_student_mastery.sql` — 学生掌握度表结构改造（多学科维度 + 层级聚合表），详见下方"学生知识图谱掌握度闭环"说明

启动 Backend：

```powershell
cd e:\PythonProject\chuma\backend
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8006

# 验证 Backend 是否启动（新开一个终端），预期返回：{"status":"ok"}
curl http://localhost:8006/health

# 检查 Backend 文档页（浏览器打开）
http://localhost:8006/docs

# 检查后端端口
netstat -ano | findstr :8006
```

## 安装 Frontend 依赖并启动

```powershell
cd e:\PythonProject\chuma\frontend
npm install
```

启动前端：

```powershell
cd e:\PythonProject\chuma\frontend
npm run dev

# 验证前端是否启动（浏览器打开）
http://localhost:8000

# 检查前端端口
netstat -ano | findstr :8000
```

## 功能页面检查

聊天功能检查：

```powershell
# 检查 LLM 模型服务是否运行（vLLM / 远程 API）
netstat -ano | findstr :8002

# 打开聊天页面
http://localhost:8000/student/chat
```

OCR 功能检查：

```powershell
# 检查 OCR 模型服务是否运行
netstat -ano | findstr :8004

# 打开 OCR 页面
http://localhost:8000/student/ocr
```

知识图谱页面：

```
http://localhost:8000/student/kg-pipeline    （图谱构建流水线）
http://localhost:8000/student/knowledge      （图谱浏览与探索）
```

仪表盘（含 AI 学习分析）：

```
http://localhost:8000/student/dashboard
```

学习规划（按学科分别制定）：

```
http://localhost:8000/student/plan
```

题目练习：

```
http://localhost:8000/student/practice
```

互动专区：

```
http://localhost:8000/student/interactive
```

教师端功能：

```
http://localhost:8000/teacher/dashboard
http://localhost:8000/teacher/chat
http://localhost:8000/teacher/interactive
http://localhost:8000/teacher/subject
```

<br />

## 术语说明

> 文档中会出现不少项目内部命名用到的英文缩写。这里先统一展开，后文遇到关键文件名时也会再次注明，避免读者靠猜。

| 缩写          | 全称                                    | 中文说明                         |
| ----------- | ------------------------------------- | ---------------------------- |
| `KG` / `kg` | Knowledge Graph                       | 知识图谱                         |
| `OCR`       | Optical Character Recognition         | 光学字符识别；在本项目里更接近"文档文字识别/文档解析" |
| `SSE`       | Server-Sent Events                    | 服务端向浏览器持续推送数据的流式通信方式         |
| `API`       | Application Programming Interface     | 应用程序接口                       |
| `JWT`       | JSON Web Token                        | 登录令牌/认证令牌                    |
| `LLM`       | Large Language Model                  | 大语言模型                        |
| `GNN`       | Graph Neural Network                  | 图神经网络                        |
| `RAG`       | Retrieval-Augmented Generation        | 检索增强生成                       |
| `GraphRAG`  | Graph Retrieval-Augmented Generation  | 基于图结构或图谱检索的检索增强生成            |
| `AGE`       | Apache AGE                            | 基于 PostgreSQL 的图数据库扩展        |
| `ORM`       | Object-Relational Mapping             | 对象关系映射                       |
| `ASGI`      | Asynchronous Server Gateway Interface | Python 异步 Web 接口规范           |
| `MCP`       | Model Context Protocol                | 模型上下文协议——为大模型提供标准化的工具调用接口    |
| `ReAct`     | Reasoning + Acting                    | 推理与行动交替进行的 Agent 循环模式        |
| `UI`        | User Interface                        | 用户界面                         |
| `URL`       | Uniform Resource Locator              | 网络地址                         |
| `SQL`       | Structured Query Language             | 结构化查询语言                      |
| `BFS`       | Breadth-First Search                  | 广度优先搜索（用于子图提取）              |

***

## 一、先给结论

### 1.1 这个项目现在到底是什么

这是一个面向 **计算机科学学习场景** 的 Web 全栈项目，目标是做成"学生侧助学 + 教师侧助教"的 AI 平台。\
当前代码仓库已经形成了 **前端 + 后端 + AI 引擎** 三层结构，真正打通并有明确代码实现的，主要有以下链路：

1. **AI 助教聊天（ReAct Agent 模式）**
   - 前端支持"快速回答"和"Agent 深度思考"两种聊天模式
   - Agent 模式：LLM 自主决定调用知识图谱查询（`search_kg`）、文档检索（`read_document`）、数据库查询（MCP 工具）等工具，通过多步推理（ReAct 循环）完成复杂问答
   - AI 服务内建 `AgentOrchestrator`（Agent 编排器），管理工具注册、分发和执行
   - 命中知识点时前端自动展示关联子图

2. **OCR 文档解析**
   - 前端可以上传 PDF / 图片
   - 后端把文件转发给 AI 服务
   - AI 服务内部用 `mineru` 跑异步 OCR 任务

3. **知识图谱构建与层次化浏览**
   - 前端上传文档发起图谱构建任务
   - 后端维护图谱元数据和任务状态
   - AI 服务执行"OCR → Markdown 切片 → LLM 抽取实体关系 → NetworkX 合并 → Apache AGE 落库"
   - 前端支持**层次化钻取浏览**：顶层章节（ECharts 2D 力导向图） → 子节 → 内层知识点（3D 立体球形图）
   - 章节父子关系通过"包含"边自动解析（`kgHierarchy.ts`），支持逐层下钻

4. **AI 学习分析（ReAct Agent 驱动）**
   - 学生在仪表盘点击"开始 AI 分析"，Agent 自主收集三个维度数据：
     - 知识图谱掌握度（`student_knowledge_mastery` 表）
     - 错题记录及知识点分布（`exercise_records` + `questions` 表）
     - 个人评级（`students.stu_level` 字段）
   - Agent 综合所有可用数据，生成包含总体概述、薄弱环节、改进建议、优先攻克知识点的结构化分析报告
   - 分析完成后，综合评级（A/B/C/D/E）会同步写回 `students.stu_level` 字段，并更新 `updated_at` 为当前时间

5. **学习规划（ReAct Agent 驱动，按学科分别制定）**
   - 学生在学习规划页（`/student/plan`）触发，Agent 为 courses 表每门学科分别制定规划
   - 综合四个维度数据（各占 25% 权重，动态调整）：
     - 学生端 AI 分析内容（`evaluation_analysis` 中 `publisher_name='AI'`）
     - 学生自身知识图谱（`student_knowledge_mastery` 按学科 kg_id 过滤）
     - 习题情况（`exercise_records` + `questions` 按 course_id：题库题目总数 + 已做题目数 → 各科进度）
     - 老师意见与评估（`evaluation_analysis` 中 `publisher_name != 'AI'`）
   - 兜底机制：数据库异常 → 前端显示用户友好错误；可用维度 ≤2 → 提示缺失维度；可用维度 ≥3 → 生成细粒度规划（权重动态调整为 1/N）

6. **用户认证与个人信息管理**
   - 学生/教师注册、登录、JWT 鉴权
   - 前端路由守卫（`guards.ts`）基于角色（student/teacher/admin）控制页面访问
   - 仪表盘个人信息编辑（姓名、邮箱、性别、密码、座右铭）

7. **题库练习系统**
   - PostgreSQL 中已导入 2484 道考研题目（数据结构 637 + 计算机组成原理 644 + 操作系统 645 + 计算机网络 558）
   - 仪表盘按学科展示进度跑道、随机推荐新题和巩固题
   - 做题提交、自动判分、做题记录持久化

8. **互动专区与消息系统**
   - 学生端/教师端互动问答列表
   - 消息通知（未读红点徽标）
   - 互动详情页（InteractiveDetail）

### 1.2 当前项目的真实完成度

按"代码已经实现"而不是"PRD 规划"来判断：

| 模块               | 当前状态       | 说明                                                            |
| ---------------- | ---------- | ------------------------------------------------------------- |
| 前端整体框架           | **已完成**    | 三套布局（学生/教师/管理员）、路由守卫、状态管理、API 分层齐全                     |
| 用户认证系统           | **已完成**    | 学生/教师注册登录、JWT 鉴权、路由守卫、个人信息编辑                           |
| 聊天功能（Agent 模式）   | **可用**     | ReAct Agent 编排器 + 工具注册中心 + MCP 客户端 + 知识图谱/文档/数据库三类工具     |
| 聊天功能（快速模式）       | **可用**     | SSE 流式输出，思考过程与正式答案分离展示                                     |
| OCR 解析           | **可用**     | 前后端和 AI 任务管理链路都已实现                                          |
| 知识图谱构建           | **主链路已实现** | 构建任务、状态轮询、AGE 写入、层次化可视化查询已具备                             |
| 知识图谱层次化浏览        | **可用**     | 章节父子关系解析 → 2D 逐层钻取 → 3D 知识点球面图，含节点搜索和详情面板               |
| 图谱元数据管理          | **可用**     | PostgreSQL 中已有 `kg_graphs` 表和对应 CRUD 服务                       |
| 仪表盘              | **已完成**    | 个人信息卡片、AI 学习分析面板、学科进度跑道、新题/巩固题推荐跳转                      |
| 题库练习             | **主链路已实现** | 2484 道题目已入库、按学科浏览、做题提交判分、做题记录持久化。提交时按题型填充 exercise_records 全部字段：客观题（single_choice/multiple_choice/T_or_F）判 do_isTrue、主观题（Fill_blanks 按空给分 / Q_A 大模型评分）判 do_score，kg_id/kg_node_name 从题库继承，iserror_firstly 历史首次判定 |
| 做题记录             | **部分实现**   | 提交记录持久化已实现，记录浏览和归因分析界面已有但功能有限                             |
| 知识图谱掌握度闭环       | **主链路已实现** | 学生按学科维系多门学科知识图谱掌握度。做题提交 → 更新知识点掌握度（客观题 5/0、主观题按得分映射、加权平均）→ 从 AGE 图解析章节层级 → 知识点平均聚合出小节/章节/学科掌握度。前端知识图谱节点球中央显示掌握度百分比数字。表结构经 `docs/sql/003_student_mastery.sql` 改造（`student_knowledge_mastery` 增加 `course_id` 维度 + 答题统计，新增小节/章节/学科掌握度表） |
| 互动专区             | **UI 已完成**  | 列表/分页/详情页已搭建，后端数据接口仍为 Mock                               |
| 学习计划             | **主链路已实现** | ReAct Agent 按学科分别制定规划，综合四维度（AI 分析/知识图谱/习题/老师意见）等权加权，含兜底机制 |
| 教师端               | **部分实现**   | 学情仪表盘、AI 聊天、互动专区、学科管理页面已搭建；班级学情仪表盘的班级/学科下拉框已按登录教师身份从数据库动态读取（含学生数量），学生进度与评级列表已按所选班级+学科动态展示（进度取自 `student_course_mastery.course_process`，评级取自 `students.stu_level`），疑难知识点词云已按所选班级+学科错题知识点占比动态展示（中间大四周小、多色）；AI 评级/平均分/掌握度仍为静态占位；作业批改/学生管理/预警等仍为占位 |
| GraphRAG 文档检索     | **部分实现**   | `read_document` 工具已接入 Agent，pgvector 已配置，RagPipeline 骨架已有      |
| GNN 推荐           | 仅骨架        | GNN 依赖和接口预留了，但模型、训练、推理都没实现                                |
| 定时任务             | 仅骨架        | APScheduler 已注册 daily_profile 和 daily_question，但内部逻辑未实现       |

### 1.3 不要被旧文档误导的几个关键事实

1. **前端早已不只是聊天页**
   - 已有三套完整布局：`LayoutStudent`（Bento 悬浮面板 + 8 入口）、`LayoutTeacher`（4 入口）、`LayoutAdmin`（4 入口）
   - UI 采用浅灰绿护眼色背景 (`#bfc5bb`) + 毛玻璃模糊滤镜 + 动态背景特效（`FloatingLines`）
   - 登录、注册已从占位变为完整实现，路由守卫基于角色自动拦截未登录请求

2. **后端不只是六个路由**
   - 实际注册了八个路由模块：`auth / knowledge / learning / practice / teacher / ai_gateway / ocr_gateway / kg_gateway`
   - 用户系统（注册、登录、个人信息编辑）已完全实现，不再是 pass 骨架

3. **AI 服务不只是聊天 — 已具备完整的 Agent 框架**
   - `ai/app/agent/` 模块实现了：`AgentOrchestrator`（ReAct 多步推理）、`ToolRegistry`（工具注册与调度）、`MCPClient`（远程工具连接）、上下文管理
   - 已注册三类工具：知识图谱查询（`search_kg`）、文档检索（`read_document`，基于 pgvector RAG）、数据库查询（通过 MCP Server）
   - `ai/app/api/analysis.py`：基于 ReAct Agent 的学生多维学习分析

4. **数据库表结构已大幅扩展**
   - 从只有 `kg_graphs` 一张表扩展到 9 张表（含 students、teachers、courses、questions、exercise_records、student_course_mastery、student_knowledge_mastery、teacher_student）
   - `docs/sql/001_create_tables.sql` 为完整建表脚本
   - `docs/sql/002_insert_data.sql` 含 2484 道考研选择题数据

5. **知识图谱浏览已支持层次化钻取**
   - 不再只是平面展示，而是分区了章节层（2D ECharts）和知识点层（3D 立体球面图）
   - `kgHierarchy.ts` 通过分析"包含"边自动解析章节父子关系和知识点归属

6. **Docker 描述并不完全自洽**
   - 根目录 `docker-compose.yml` 里声明了 `frontend` 要构建 `./frontend`，但仓库中**没有** `frontend/Dockerfile`
   - 推荐只用 `docker-compose.middleware.yml` 启动 PostgreSQL + Redis + ChromaDB

7. **知识图谱功能依赖 Apache AGE 扩展**
   - 代码里明确执行 `LOAD 'age';`，但仓库没有提供安装 AGE 扩展的 Dockerfile 或初始化脚本

8. **AI 端口存在本地/远程口径差异**
   - 本地开发：AI 服务端口 8009（本文档启动命令）
   - 远程服务器：AI 服务端口 8001（CLAUDE.md 描述）
   - 前端 Vite 代理 `/api` 到后端 8006，后端通过 `AI_SERVICE_URL` 环境变量配置 AI 服务地址

***

## 二、项目整体技术栈

### 2.1 技术总览

| 层级            | 技术                           | 现在是否真正参与运行 | 在项目中的作用                                               |
| ------------- | ---------------------------- | ---------- | ----------------------------------------------------- |
| 前端框架          | Vue 3 + TypeScript           | 是          | 写前端页面、组件、交互逻辑                                         |
| 路由            | Vue Router + 路由守卫           | 是          | 管理单页应用 URL 与页面映射，基于角色（student/teacher/admin）的访问控制      |
| 状态管理          | Pinia                        | 是          | 管理登录态、图谱数据、学习状态                                       |
| UI 组件库        | Element Plus + lucide-vue-next | 是         | 页面布局、表单、按钮、上传、结果提示、图标等                               |
| 2D 图可视化       | ECharts                      | 是          | 渲染知识图谱章节关系图（外层 2D 力导向布局）                              |
| 3D 图可视化       | 3d-force-graph (Three.js)    | 是          | 渲染内层知识点立体球形图谱及粒子流连线                                   |
| CSS 工具        | Tailwind CSS                 | 是          | 原子化 CSS 类名，配合 `clsx` + `twMerge` 管理组件样式                |
| 构建工具          | Vite                         | 是          | 本地开发服务器（端口 8000）、前端打包，`/api` 代理到 `http://localhost:8006` |
| HTTP 客户端      | Axios                        | 是          | 常规 API 请求（自动注入 JWT token，401 统一拦截）                    |
| SSE / fetch   | 浏览器原生 fetch                  | 是          | 聊天流式输出（含 Agent 模式的多事件类型 SSE）                          |
| 后端框架          | FastAPI                      | 是          | 提供 REST API、文件上传、SSE 代理                             |
| ASGI 服务器      | Uvicorn                      | 是          | 跑 FastAPI 服务                                          |
| 配置管理          | pydantic-settings            | 是          | 从 `.env` 读取配置                                         |
| ORM           | SQLAlchemy asyncio           | 是          | 管理 PostgreSQL 中的业务表（9 张表，含索引和外键约束）                     |
| PostgreSQL 驱动 | asyncpg                      | 是          | 后端异步连接 PostgreSQL                                     |
| 迁移工具          | Alembic                      | 是          | 管理数据库表结构变更（当前 3 个迁移）                                   |
| 安全            | python-jose + passlib/bcrypt | 是          | JWT 鉴权、密码哈希、路由依赖注入                                    |
| 微服务调用         | httpx                        | 是          | 后端转发到 AI 服务，AI 调模型接口                                  |
| 异步队列          | Redis                        | 是          | AI 任务队列、任务结果暂存                                        |
| 定时任务          | APScheduler                  | 是          | AI 服务启动时注册每日任务                                        |
| Agent 框架       | ReAct Agent + Tool Registry  | 是          | 多步推理循环、工具注册与调度、MCP 远程工具集成                            |
| MCP 协议        | mcp + FastMCP                | 是          | 标准化工具接口，连接 AI Agent 与数据库能力                             |
| OCR 引擎        | mineru                       | 是          | PDF / 图片解析为 Markdown 等结果                              |
| 大模型调用         | httpx 封装的 OpenAI 兼容接口        | 是          | 统一调用聊天模型和知识图谱抽取模型（支持 tool_calls）                       |
| 向量库           | ChromaDB + pgvector          | 部分接入      | ChromaDB 为 GraphRAG 预留；pgvector 已配置用于文档切片检索              |
| 深度学习          | PyTorch / PyG                | 暂未真正接入主链路  | 为未来 GNN 预留                                          |
| 图处理中间层        | NetworkX                     | 是          | 把多个切片抽取结果合并成一张图                                       |
| 图数据库          | Apache AGE                   | 是          | 存储知识图谱节点和边，并支持 Cypher 查询                                |
| 容器编排          | Docker Compose               | 部分可用       | `docker-compose.middleware.yml` 可用于启动中间件                   |

### 2.2 各技术在项目中的具体应用场景

#### Vue 3 + TypeScript

- **是什么**：前端框架 + 类型系统
- **项目里怎么用**：`frontend/src/pages/` 下所有页面、`frontend/src/components/` 下所有组件都是 Vue 单文件组件（SFC）
- **具体场景**：聊天页、OCR 上传页、图谱浏览页、仪表盘、题目练习、互动专区

#### Vue Router + 路由守卫

- **是什么**：前端路由管理器 + 导航守卫
- **项目里怎么用**：`frontend/src/router/index.ts`（路由定义）+ `frontend/src/router/guards.ts`（守卫逻辑）
- **路由守卫工作逻辑**：
  1. `beforeEach` 拦截每次路由跳转
  2. 检查目标路由 `meta.public`（登录/注册页免认证）
  3. 检查 `localStorage` 中是否有 token（无 → 重定向到 `/login`）
  4. 解析用户角色，与路由 `meta.role` 比对（不匹配 → 重定向到自己的 dashboard）
  5. admin 角色可访问所有页面
- **关键依赖链**：`guards.ts` → `localStorage.getItem('token')` + `localStorage.getItem('userRole')` → 决定放行或重定向

#### Pinia

- **是什么**：前端全局状态管理
- **项目里怎么用**：
  - `stores/auth.ts` — 存 token 和用户信息（UserInfo）于内存，token 同步到 localStorage
  - `stores/knowledge.ts` — 管图谱列表、当前图谱、节点选择、搜索、删除
  - `stores/learning.ts` — 预留（progress 和 plan 字段）
  - `store/messages.ts` — 消息列表与未读数量计算

#### Element Plus

- **是什么**：前端 UI 组件库
- **项目里怎么用**：上传组件 `el-upload`、表单 `el-form`、提示 `el-alert`/`el-result`、菜单 `el-menu`、表格 `el-table`、分页 `el-pagination`、对话框 `el-dialog`、消息提示 `ElMessage`/`ElMessageBox`

#### Tailwind CSS + clsx + twMerge

- **是什么**：原子化 CSS 框架 + 类名合并工具
- **项目里怎么用**：`frontend/src/lib/utils.ts` 中的 `cn()` 函数组合 `clsx` 和 `tailwind-merge`，用于组件中动态合并 CSS 类名
- **具体场景**：布局组件中大量使用 Tailwind 原子类（如 `fixed inset-0 z-40 bg-black/20 backdrop-blur-md`）

#### ECharts（2D 力导向图）

- **是什么**：图表和关系图可视化库
- **项目里怎么用**：`KnowledgeGraph2D.vue`
- **具体场景**：把 AGE 查出的章节节点和边画成 2D 力导向图。支持节点点击钻取、搜索高亮、缩放、重置。该组件的输出是用户可交互的知识图谱。

#### 3d-force-graph & Three.js（3D 粒子特效图）

- **是什么**：3D 力导向图渲染 + 3D 渲染引擎
- **项目里怎么用**：`KnowledgeGraph3D.vue`
- **具体场景**：当用户钻取到知识点层（章节无子节或已钻取到最深层），自动切换为 3D 立体球形图。渲染不透明实体球节点和粒子流动效连线。

#### 层次化图谱解析（kgHierarchy.ts）

- **是什么**：图谱层次结构解析工具
- **工作原理**：
  1. 遍历所有边，找到 `relationship_name === '包含'` 的边
  2. `Chapter → Chapter` 的边 = 章节父子关系
  3. `Chapter → 非Chapter` 的边 = 知识点归属
  4. 不出现在任何 Chapter→Chapter 边 target 位置的 Chapter = 顶层章
- **输出 → 输入链**：`kgHierarchy.parseHierarchy()` 输出 → `getVisibleData()` 根据钻取路径过滤可视节点 → `KnowledgeGraph2D.vue` 或 `KnowledgeGraph3D.vue` 渲染

#### Vite

- **是什么**：前端开发服务器和打包工具
- **配置要点**：`vite.config.ts` 中 `server.port = 8000`，`proxy['/api'].target = 'http://localhost:8006'`
- **调用链路**：浏览器请求 `/api/xxx` → Vite 代理转发到 `http://localhost:8006/api/xxx` → Backend 处理

#### Axios

- **是什么**：前端常规 HTTP 请求工具
- **封装细节**（`request.ts`）：自动从 `localStorage` 注入 `Authorization: Bearer <token>` 头；401 响应自动清除 token 并跳转 `/login`；统一错误提示

#### FastAPI

- **是什么**：Python Web 框架
- **项目里两个实例**：
  - `backend/app/main.py`：业务 API + 网关转发（CORS 已配置，8 个路由模块）
  - `ai/app/main.py`：AI 能力 + Agent 服务（lifespan 中启动 scheduler、OCR task manager、Redis worker、MCP 客户端连接）

#### SQLAlchemy + asyncpg + PostgreSQL

- **它们的关系**：
  - PostgreSQL 存业务数据（9 张表）
  - SQLAlchemy 定义 ORM 模型和操作数据库
  - asyncpg 是 PostgreSQL 的异步驱动（异步非阻塞 I/O）
- **当前已落地的表**：`kg_graphs`、`students`、`teachers`、`courses`、`questions`（2484 条）、`exercise_records`（表名拼写为 exertcise_records）、`student_course_mastery`、`student_knowledge_mastery`、`teacher_student`（另经 `docs/sql/003_student_mastery.sql` 新增 `student_section_mastery`、`student_chapter_mastery` 层级掌握度表）

#### Alembic

- **是什么**：数据库迁移工具
- **当前迁移**：
  - `001_add_kg_graphs_table.py` — 图谱元数据表
  - `002_student_side_tables.py` — 学生/教师/课程/题库/做题记录/掌握度
  - `003_add_document_chunks.py` — 文档切片向量表（pgvector）
- **增量 SQL 脚本**：
  - `docs/sql/002_alter_tables.sql` — 为 `kg_graphs` 表新增 `course_id` 列（学科 → 图谱 映射）
  - `docs/sql/003_student_mastery.sql` — 学生掌握度表结构改造（多学科维度 + 层级聚合表）

#### JWT / python-jose / passlib

- **协作关系**：
  - `passlib + bcrypt`：前端传来的密码 → 哈希 → 与数据库存储的哈希比对（登录验证）
  - `python-jose`：验证通过后签发 JWT（含 user_id、user_type、过期时间）；每次请求从 `Authorization` 头解析 JWT
- **依赖注入链**：`get_current_user` → 解析 JWT → 查数据库获取最新用户信息 → 注入到路由处理函数

#### httpx

- **是什么**：Python HTTP 客户端（支持异步）
- **项目里两个关键使用处**：
  1. 后端网关层（`backend/app/api/*_gateway.py`）：转发前端请求到 AI 服务
  2. AI 引擎 LLM 客户端（`ai/app/engines/llm/client.py`）：调用外部模型 API（本地 vLLM / 远程大模型）

#### Redis

- **是什么**：内存数据库
- **当前用途**：
  1. 异步任务队列（key: `chuma:tasks`，worker 通过 BRPOP 阻塞获取任务）
  2. 知识图谱构建结果缓存（key pattern: `kg:result:{task_id}`）
  3. `task_id → kg_graph_id` 映射（供状态轮询使用）

#### ReAct Agent 框架（核心新增能力）

- **是什么**：基于"推理（Reasoning）+ 行动（Acting）"循环的智能体框架
- **涉及文件**：`ai/app/agent/orchestrator.py`（主循环）、`ai/app/agent/tool_registry.py`（工具注册与分发）、`ai/app/agent/mcp_client.py`（MCP 远程工具连接）、`ai/app/agent/context.py`（协程安全的上下文变量）
- **Agent 循环工作流程**（最多 10 轮）：
  1. LLM 收到用户消息和系统提示词（含可用工具列表）
  2. LLM 决定：直接回答 OR 调用某个工具
  3. 如果调用工具 → `ToolRegistry.execute()` 分发执行（本地工具直接调用，MCP 工具走 MCP 客户端）
  4. 工具结果追加到消息历史 → 回到步骤 2
  5. 如果 LLM 决定不调用工具 → 流式输出最终答案
  6. 超 10 轮未收敛 → 强制要求 LLM 基于已有信息回答
- **工具注册模式（装饰器自发现）**：`@ToolRegistry.register(name, desc, params)` 装饰函数 → 自动出现在 Agent 可用工具列表中
- **已注册工具**：
  - `search_kg`（本地）：查询知识图谱节点（调用 `kg_pipeline/queries.py`）
  - `read_document`（本地）：基于 pgvector 的文档语义检索（调用 `engines/rag/pipeline.py`）
  - MCP 数据库工具（远程）：`list_tables`、`describe_table`、`query_postgresql`

#### MCP（Model Context Protocol）

- **是什么**：为大模型提供标准化工具调用接口的协议
- **项目中的两层实现**：
  1. **MCP Server（服务提供方）**：`scripts/mcp_db_server.py`（端口 8005），基于 FastMCP + asyncpg，暴露三类工具：
     - `list_tables` — 列出所有表名
     - `get_table_schema(table_name)` — 获取表结构
     - `execute_readonly_sql(sql)` — 执行只读 SQL（防注入 + 最多返回 50 行）
  2. **MCP Client（服务消费方）**：`ai/app/agent/mcp_client.py`，连接到 Server 并注册远程工具到 `ToolRegistry`

#### pgvector（向量扩展）

- **是什么**：PostgreSQL 的向量相似度搜索扩展
- **项目里怎么用**：`003_add_document_chunks.py` 迁移创建 `document_chunks` 表（含 `embedding vector(768)` 列和 HNSW 索引）
- **与 Agent 的协作**：`read_document` 工具 → `RagPipeline` → pgvector 相似度检索 → 返回相关文档片段

#### APScheduler

- **是什么**：定时任务调度器
- **现状**：已注册 `daily_profile`（凌晨 4:00）和 `daily_question`（凌晨 4:30），调度器可工作，任务逻辑未实现

#### mineru

- **是什么**：文档解析/OCR 工具链
- **调用链**：`ai/app/ocr/service.py` → `aio_do_parse` → 输出 Markdown/JSON/图片 → 打包为 ZIP 供下载
- **也是知识图谱构建的前置步骤**：OCR → Markdown → Chunking → KG Extraction

#### LLM 调用层

- **是什么**：统一大模型访问层
- **核心文件**：`ai/app/engines/llm/client.py`（封装 HTTP 调用、重试、超时）、`ai/app/engines/llm/profiles.py`（模型端点配置）
- **四种 profile**：
  - `quick_profile()` — 内网 Qwen3.5-9B（vLLM，端口 8002）
  - `remote_profile()` — 远程大模型 API（DeepSeek / 通义千问等）
  - `deepseek_profile()` — DeepSeek 专用配置
  - `local_profile()` — 本地微调小模型（预留）

#### NetworkX

- **是什么**：Python 图结构库
- **作用**：把多个文档切片抽取出的局部图合并为一张总图，做节点去重、边校验
- **数据流**：`KGExtractor`（多切片 LLM 抽取）输出多张局部图 → `GraphBuilder`（NetworkX 合并去重）→ 输出合并图给 `AgeStorage`

#### Apache AGE

- **是什么**：基于 PostgreSQL 的图数据库扩展，支持 Cypher 查询语言
- **项目中的三层使用**：
  1. `storage.py` — 建图（`create_graph`）、写节点边（`INSERT VERTEX/EDGE`）、清图（`drop_graph`）
  2. `queries.py` — 查询全图（`MATCH ... RETURN`）、搜索节点（按名称/类型匹配）
  3. `ai/app/api/kg.py` — 对外暴露图查询接口（供前端可视化使用）

#### ChromaDB（预留）

- **是什么**：向量数据库
- **当前状态**：docker 容器和依赖已准备，但主链路尚未接入。未来用于 GraphRAG 混合检索

***

## 三、项目运行时各技术的相互关系

这一节重点讲"技术之间怎么配合"，按功能链路逐一展开。

### 3.1 聊天链路（Agent 模式 — 当前最主要）

```
Vue 页面 (Chat.vue) + ChatInput.vue
  → useChat.ts 管理消息列表与模式切换
  → api/ai.ts 用 fetch 建立 SSE 连接（POST /api/ai/chat/agent）
  → Backend ai_gateway.py 转发请求（注入 X-Service-Token）
    → AI chat.py 创建 AgentOrchestrator
      → AgentOrchestrator.run() 进入 ReAct 循环：
        ├── LLM 决定调用工具
        │   ├── search_kg → kg_pipeline/queries.py → Apache AGE
        │   ├── read_document → engines/rag/pipeline.py → pgvector
        │   └── MCP 工具 → mcp_client.py → scripts/mcp_db_server.py → PostgreSQL
        ├── ToolRegistry.execute() 分发执行
        └── 工具结果追加到消息历史 → 继续循环
      → LLM 决定不调用工具 → 流式输出最终答案
  → SSE 事件流回到前端：
    ├── "tool_used" → 显示工具调用动画
    ├── "tool_result" → 显示工具结果摘要
    ├── "kg_hit" → 触发子图面板展示（ChatSubgraphPanel + SubgraphChart）
    └── "content" → 逐段渲染 Markdown/公式
  → ChatMessage.vue 渲染 Markdown / 数学公式 / 思考过程
```

**技术协作关系**：
- **fetch + SSE**：建立长连接，逐事件推送到前端
- **AgentOrchestrator**：维护消息历史 + 工具定义 → 传给 LLM → 解析 tool_calls → 通过 ToolRegistry 分发执行 → 工具返回值追加到历史 → 循环
- **ToolRegistry**：统一管理本地和 MCP 两种工具来源，统一返回 OpenAI function-calling 格式
- **contextvars**：`current_kg_graph_ids` 和 `current_graph_names` 协程安全地传递教材上下文（不污染全局变量）
- **kg_hit 事件 → useSubgraph.ts**：BFS 双向搜索提取命中节点的上下游子图 → `SubgraphChart.vue` 渲染 ECharts 子图

### 3.2 聊天链路（快速模式）

```
Vue 页面 (Chat.vue)
  → useChat.ts
  → api/ai.ts 用 fetch 建立 SSE
  → Backend ai_gateway.py
  → AI chat.py 直接调用 LLMClient.stream()
  → 外部/内网模型服务
  → SSE 流回到前端
  → ChatMessage.vue 渲染（区分思考过程与正式答案）
```

### 3.3 OCR 链路

```
Vue 页面 (OcrParse.vue)
  → useOcr.ts 提交和轮询
  → frontend/api/ocr.ts
  → backend/api/ocr_gateway.py（多文件表单转发）
  → ai/ocr/router.py
  → AsyncTaskManager（异步任务编排，含并发控制、状态管理、过期清理）
  → mineru 文档解析
  → 结果 ZIP 保存在 AI 服务输出目录
  → 前端轮询状态并展示/下载
```

### 3.4 知识图谱构建链路

```
Vue 页面 (KgPipeline.vue)
  → useKgPipeline.ts 提交和轮询
  → frontend/api/kgPipeline.ts
  → backend/api/kg_gateway.py
    → kg_graph_service.py 在 PostgreSQL 创建 kg_graphs 记录
    → Redis 保存 task_id → kg_graph_id 映射
    → httpx 转发到 AI /kg/build
  → AI kg.py 把任务压入 Redis 队列 (chuma:tasks)
  → worker.py BRPOP 取出任务
  → tasks/kg_build.py → KGPipeline.run_from_file()
    ├── OCR（mineru 解析文档 → Markdown）
    ├── MarkdownChunker（按标题/段落/句子切片）
    ├── KGExtractor（每个切片调 LLM 抽取实体和关系）
    ├── GraphBuilder（NetworkX 合并所有局部图，节点去重，边校验）
    └── AgeStorage（写入 Apache AGE：建图 → 批量写节点 → 批量写边）
  → 查询接口从 AGE 读图
  → KnowledgeExplore.vue 用层次化视图展示
```

**这是当前仓库最有价值的一条主链路**，因为它把很多技术串起来了：
- **Vue + Element Plus**：上传文件、看任务进度
- **FastAPI Backend**：建元数据、转发、同步任务状态
- **SQLAlchemy + PostgreSQL**：保存图谱记录
- **Redis**：做任务队列和状态缓存
- **FastAPI AI + Worker**：执行异步图谱构建
- **mineru**：先把文档转成 Markdown
- **LLM**：从文本里抽实体和关系（少量样本提示词 + JSON 结构化输出）
- **NetworkX**：合并多块图结果（节点去重逻辑：同名 + 同类型 → 合并属性）
- **Apache AGE**：图数据最终存储（Cypher 查询）
- **ECharts + 3d-force-graph**：前端层次化可视化

### 3.5 AI 学习分析链路（ReAct Agent）

```
Vue Dashboard.vue 点击"开始 AI 分析"
  → api/analysis.ts (POST /api/ai/analysis/stu_analysis?stu_id=X)
  → Backend ai_gateway.py 转发
  → AI analysis.py → analyze_student(stu_id)
    → 创建 AgentOrchestrator（配备 stu_analysis_db 工具集）
    → Agent 自主调用三个数据查询工具（顺序由 LLM 自主决定）：
      ├── query_student_level：查询 students.stu_level
      ├── query_knowledge_mastery：查询 student_knowledge_mastery（掌握度最低节点）
      └── query_wrong_exercises：查询 exercise_records + questions（错题分布）
    → 每个工具返回 {success, data, summary} 结构
    → 后处理代码从 data 提取 dimensions_detail
    → Agent 基于 summary 生成 analysis（总体概述/薄弱环节/改进建议/优先攻克知识点）
  → 返回 StuAnalysisResult（含维度可用性 + 综合分析）
  → Dashboard 展示分析结果（评级 / 维度标签 / 四段分析内容 / 优先攻克列表）
```

**技术协作关系**：
- **ReAct Agent**：不是简单的"调 API 返回结果"，而是 LLM 自主决定调用哪些工具、以什么顺序、是否重试
- **双消费者设计**：工具返回的 `data` 给后处理代码提取结构化数据，`summary` 给 LLM 做推理——同一份结果服务两个消费者
- **psycopg2 直连**：分析工具使用 psycopg2（同步连接）直连 PostgreSQL，复用 AGE 数据库配置

### 3.6 学习规划链路（ReAct Agent，按学科分别制定）

```
Vue LearningPlan.vue（/student/plan，页面加载自动生成）
  → api/learningPlan.ts (POST /api/ai/analysis/learning_plan, body={stu_id}, timeout=120s)
  → Backend ai_gateway.py 转发 → AI learning_plan.py → generate_learning_plan(stu_id)
    → LearningPlanAgent.generate(stu_id)
      ├── Phase 1: 确定性查询学科列表（query_subjects，courses 表全部学科）
      └── Phase 2: 对每门学科分别处理（_process_subject）：
          ├── ReAct 循环收集四维度数据（LLM 自主调用 4 个工具）：
          │   ├── query_ai_analysis      → evaluation_analysis 中 publisher_name='AI'
          │   ├── query_knowledge_mastery→ student_knowledge_mastery 按学科 kg_id 过滤
          │   ├── query_exercise_progress→ exercise_records + questions 按 course_id（题库题目总数 + 已做题目数 → 各科进度）
          │   └── query_teacher_opinion  → evaluation_analysis 中 publisher_name != 'AI'
          │   （循环结束后确定性补齐 LLM 漏调的工具，保证兜底判定完整）
          ├── 确定性后处理：构建 dimensions_detail + 计算权重（等权 1/N）
          ├── 兜底判定：
          │   ├── 任一维度 db_error → status=db_error（前端显示用户友好错误）
          │   ├── 可用维度 ≤2 → status=insufficient（前端提示缺失维度）
          │   └── 可用维度 ≥3 → 用权重约束提示词生成细粒度规划（status=ok）
          └── 规划生成：build_plan_system_prompt 约束四维度等权，输出 JSON
  → 返回 LearningPlanResult（subjects 列表，每学科含 status/weights/dimensions_detail/plan）
  → LearningPlan.vue 展示各学科规划卡片（总体目标/薄弱知识点/每周计划/优先突破点/老师意见补充）
```

**技术协作关系**：
- **按学科分别规划**：不是把四门 408 科目揉在一起，而是对 courses 表每门学科独立收集数据、独立生成规划
- **四维度等权**：AI 分析 / 知识图谱 / 习题情况 / 老师意见各占 25%，缺失维度时权重动态调整为 1/N（确定性计算，不交给 LLM）
- **兜底机制**：工具返回 `error_type` 区分 `db_error`（数据库异常）与 `no_data`（无数据）；`no_data` 前端提示"可能用户还没有开展学习哦~"，`db_error` 前端显示用户友好错误
- **psycopg2 直连**：学习规划工具使用 psycopg2 直连 PostgreSQL，复用 AGE 数据库配置

### 3.7 用户认证链路

```
Vue Login.vue（表单提交）
  → api/auth.ts (POST /api/auth/login)
  → backend/auth.py → AuthService.login()
    → 查 students/teachers 表（按 email + user_type）
    → passlib 验证密码哈希
    → python-jose 签发 JWT（含 user_id、user_type、exp）
  → 返回 {access_token, token_type, user_info}
  → authStore.setToken(token) + localStorage.setItem('token', token)
  → router.push('/student/dashboard') 或 '/teacher/dashboard'

每次后续请求：
  → request.ts 拦截器自动注入 Authorization: Bearer <token>
  → Backend get_current_user 依赖解析 JWT → 查数据库获取最新用户信息
  → 注入到路由处理函数

路由守卫：
  → guards.ts beforeEach 检查 token 存在性 + 角色匹配 + 路径重定向
```

### 3.8 登录 → Dashboard → 做题链路

```
用户登录 → Dashboard 加载：
  ├── loadUserInfo() → GET /api/auth/me → 显示姓名/邮箱/性别/评级
  ├── fetchCourses() → GET /api/practice/courses → 渲染学科四宫格
  └── 每个学科并行：
      ├── fetchDashboardNewQuestion(course_id) → 随机获取一道未做过的题
      └── fetchDashboardRecordQuestion(course_id) → 随机获取一道已做过的题

点击"跳转练习" → /student/practice/panel?questionId=X&module=Y&idList=...
  → Practice.vue 加载题目（题目描述/选项/答案）
  → 用户作答 → POST /api/practice/submit
  → PracticeService.submit_exercise()：
    ├── 从 questions 表继承 kg_id / kg_node_name（不依赖前端传参）
    ├── 按题型判定 do_isTrue / do_score：
    │     ├── single_choice / multiple_choice / T_or_F：do_isTrue 生效（比较答案），do_score 置 null
    │     ├── Fill_blanks：do_score 按空给分（满分10/填空总数 × 填对数量，答案用英文逗号分隔），do_isTrue 置 null
    │     └── Q_A：do_score 由大模型评分（满分10分，float），do_isTrue 置 null
    ├── iserror_firstly 历史首次判定：客观题首次做错为 true；主观题首次得分低于 6 分为 true；重复提交保留首次判定
    ├── 写入 exercise_records 表（所有字段按规则填充）
    └── 调用 MasteryService.update_knowledge_mastery() 更新知识点掌握度（闭环起点）：
        ├── 客观题答对 5.0 分 / 答错 0.0 分；主观题 do_score/10*5 映射
        ├── 多次作答取加权平均（避免一次失误清零）
        └── 更新 answered_count / correct_count 统计
  → 前端知识图谱页 GET /api/learning/mastery/hierarchy?course_id=X
    → MasteryService.get_mastery_hierarchy() 从 AGE 图解析章节层级
    → 知识点掌握度平均 → 小节 → 章节 → 学科（闭环聚合）
    → 节点球中央显示掌握度百分比数字（如 68%），掌握度越高数字越大
```

### 3.9 层次化图谱浏览链路

```
KnowledgeExplore.vue 加载图谱列表
  → 用户选择图谱 → loadGraphData(graphName)
  → GET /api/kg/data/{graph_name} → AI AGE 查询 (MATCH ... RETURN)
  → parseHierarchy(data) 解析章节层级：
    ├── 筛选"包含"边
    ├── Chapter→Chapter → 父子关系 (chapterChildren Map)
    ├── Chapter→非Chapter → 知识点归属 (chapterKnowledgePoints Map)
    └── 找出顶层章（不出现在任何父子关系 target 中的 Chapter）
  → getVisibleData(data, drillPath, hierarchy) 过滤当前层级可视数据
  → 并行加载掌握度：GET /api/learning/mastery/hierarchy?course_id=X → 构建 节点名→掌握度 映射
  → 顶层：ECharts 2D 图展示顶层章节点（节点中央显示掌握度百分比数字）
  → 用户点击章节点 → 钻取到子节层（2D 图更新为当前章 + 子节）
  → 再点击 → 钻取到知识点层（自动切换为 3D 立体球面图，节点球中央显示掌握度百分比数字）
  → 点击非 Chapter 节点（如 Concept/Algorithm）→ GraphDetailPanel 展示详情
```

### 3.10 当前"已接入"和"预留未接入"的技术关系

| 技术                                                | 当前状态        | 说明                     |
| --------------------------------------------------- | ----------- | ---------------------- |
| Vue / FastAPI / Redis / PostgreSQL / httpx / mineru / AGE / ECharts / Agent | **已接入主链路**  | 这些是当前项目真正跑得起来的核心组合     |
| pgvector + document_chunks                          | **已配置**     | read_document 工具已有骨架实现   |
| ChromaDB                                            | 预留          | 未来给 GraphRAG 混合检索用     |
| PyTorch / PyG                                       | 预留          | 未来给 GNN 推荐用            |
| OpenAI SDK                                          | 依赖已装但当前代码未用 | 当前模型调用是自己用 httpx 封装的   |
| Monaco Editor                                       | 包已装但当前页面没用  | 未来可能用于 SQL/代码编辑        |

***

## 四、项目当前已经做到什么程度

### 4.1 已经有明确代码闭环的功能

#### 1. AI 助教聊天（双模式）

已完成：
- 前端聊天页面（消息列表、输入框、模式切换）
- **Agent 深度思考模式**：ReAct Agent 编排器 + 三大工具类 + 多步推理循环 + SSE 多事件流
- **快速回答模式**：直接调 LLM 流式输出
- 思考过程与正式答案分离展示
- 命中知识点时自动展示关联子图（BFS 双向搜索提取上下游子图）
- 工具调用过程可视化（tool_used / tool_result 事件）

还没完成：
- Agent 模式尚未在 GraphRAG 层面做混合检索（当前仅依赖知识图谱查询 + 文档检索，ChromaDB 未接入）
- 用户对话持久化（无聊天历史存储）

#### 2. OCR 文档解析

已完成：
- 前端上传 PDF / JPG / PNG + 解析参数配置
- 异步任务提交 + 状态轮询 + 结果 ZIP 下载

还没完成：
- 前端未将 OCR 结果直接可视化展示成详细页面

#### 3. 知识图谱构建与层次化浏览

已完成：
- 文档上传 → 图谱构建任务 → 状态轮询
- PostgreSQL 图谱元数据管理（CRUD）
- Redis 任务队列
- 完整构建流水线：OCR → Markdown 切片 → LLM 抽取 → NetworkX 合并 → AGE 落库
- 前端图谱列表、选择、搜索、删除
- **层次化钻取浏览**：章节层 2D → 子节层 2D → 知识点层 3D
- 节点详情侧边面板（GraphDetailPanel）
- 节点搜索与高亮
- **掌握度闭环可视化**：做题更新知识点掌握度 → 聚合出小节/章节/学科掌握度 → 节点球中央显示掌握度百分比数字

#### 4. 用户认证系统

已完成：
- 学生注册（`POST /api/auth/register/student`）
- 教师注册（`POST /api/auth/register/teacher`）
- 统一登录（`POST /api/auth/login`，区分 user_type）
- JWT 鉴权 + 路由守卫（基于角色的页面访问控制）
- 个人信息查询（`GET /api/auth/me`）
- 个人信息编辑（`PUT /api/auth/profile`）

#### 5. 学习仪表盘

已完成：
- 个人信息卡片（头像、姓名、邮箱、评级、座右铭）
- AI 学习分析面板（ReAct Agent 驱动，三个维度综合评估）
- 学科四宫格（进度跑道可视化、最新题目推荐、巩固题目推荐）
  - 学科进度取自 `student_course_mastery.course_process`（0~1），经 `/learning/dashboard-progress` 接口读取；表中无该学科记录或进度为 0 时一律默认为 0
- 个人信息编辑弹窗（姓名/邮箱/性别/密码/座右铭）
- 学科知识图谱快捷跳转（点击学科"知识图谱"按钮 → 跳转 `/student/knowledge?module=学科ID`，KnowledgeExplore 通过 `kg_graphs.course_id` 找到该学科绑定的图谱并自动选中）

#### 6. 题库练习

已完成：
- 2484 道题目入库（408 考研四学科）
- 按学科浏览题目
- 做题提交 + 自动判分 + 得分计算
- 做题记录持久化（exercise_records 表）
- Dashboard 随机新题/巩固题推荐

### 4.2 半成品模块

1. **互动专区**
   - 前端列表、分页、详情页已搭建
   - 后端数据接口仍为 Mock
2. **文档检索（read_document）**
   - pgvector 已配置，document_chunks 表已建立
   - RagPipeline 骨架已存在
   - 但文档向量化和入库的完整流程尚未实现
3. **定时任务**
   - 调度器可工作
   - 真正的每日画像、每日一题逻辑未实现

### 4.3 主要还停留在骨架的模块

1. 做题记录浏览与归因分析（提交已实现，展示和分析有限）
2. 教师端作业批改/学生管理/预警
3. GraphRAG 混合检索（ChromaDB）
4. GNN 推荐与训练
5. 错题本（WrongBook.vue 占位）

### 4.4 当前仓库里最值得注意的现实风险

| 风险点                  | 事实                                                                |
| -------------------- | ----------------------------------------------------------------- |
| Docker 前端镜像缺失        | `docker-compose.yml` 有 frontend build，但仓库没有 `frontend/Dockerfile` |
| AI 端口口径不一致           | 本文档本地启动用 8009，CLAUDE.md 远程用 8001，两者需在 `.env` 中正确配置 `AI_SERVICE_URL` |
| 图谱依赖 AGE 环境          | 仓库未提供 AGE 安装流程自动化配置                                             |
| Chroma / GNN 仍是规划态    | 依赖和骨架在，但核心逻辑没有完成                                                |
| exercise_records 表名拼写 | 迁移文件中拼写为 `exertcise_records`（少了一个 c），如手工写 SQL 需注意              |

***

## 五、目录与文件逐项说明

> 说明：下面按"项目源码、配置、脚本、文档"逐项列出。\
> `backend/venv/`、`ai/venv/`、`frontend/node_modules/` 属于第三方依赖目录，不逐文件拆解。

### 5.1 根目录

| 路径                              | 作用                                                                         |
| ------------------------------- | -------------------------------------------------------------------------- |
| `.env`                          | 当前本地或运行环境的真实配置文件，保存数据库、AI 地址、JWT、AGE 等参数。                              |
| `.env.example`                  | 环境变量模板，说明项目运行需要哪些配置项。                                                      |
| `.gitignore`                    | Git 忽略规则，排除虚拟环境、构建产物、模型文件、`.env` 等。                                        |
| `CLAUDE.md`                     | 仓库协作文档，记录远程服务器端口、服务部署方式、开发约定。它反映的是"团队/工具工作说明"，不是业务运行代码。                    |
| `docker-compose.yml`            | 根级编排文件，声明了 frontend、backend、ai、postgres、redis、chromadb 六个服务。当前前端镜像构建条件不完整。 |
| `docker-compose.middleware.yml` | 只启动 postgres、redis、chromadb 的中间件编排文件。                                      |
| `README.md`                     | 项目简介和快速启动文档，但其中部分描述比当前代码更偏规划态。                                             |
| `新手入门指南.md`                   | 本文档。                                                                       |
| `template.txt`                  | 模板文件（用途待确认）。                                                               |
| `scripts/test_deploy.py`        | 独立模型部署测试脚本，用 `transformers + torch` 直接加载远程模型路径验证推理环境。                      |

### 5.2 `docs/`

| 路径                                                             | 作用                                                                                  |
| -------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `docs/PRD.md`                                                  | 产品需求文档（PRD = Product Requirements Document）。描述项目目标、用户故事、远期功能。                      |
| `docs/architecture-diagram.html`                               | 架构图 HTML 文件，用于可视化展示系统结构。                                                            |
| `docs/XH-202620_面向一流学科建设的学科垂类大模型与创新应用开发.md`                    | 赛事/题目背景文档。                                                                          |
| `docs/refs/Cognee源码分析报告.md`                                    | 外部方案/参考实现分析资料。                                                                      |
| `docs/sql/001_create_tables.sql`                               | **完整建表脚本**（10 张表：kg_graphs、students、teachers、courses、questions、exercise_records、student_course_mastery、student_knowledge_mastery、teacher_student、evaluation_analysis）。适用于全新数据库。 |
| `docs/sql/002_insert_data.sql`                                 | **题库数据插入脚本**（2484 道 408 考研选择题：数据结构 637 + 计算机组成原理 644 + 操作系统 645 + 计算机网络 558）。 |
| `docs/sql/003_student_mastery.sql`                             | **学生掌握度表结构改造脚本**。为 `student_knowledge_mastery` 新增 `course_id`（学科维度）、`answered_count`/`correct_count`（答题统计），主键重建为 `(stu_id, course_id, kg_node_name)`；并新增 `student_section_mastery`、`student_chapter_mastery`、`student_course_mastery` 三张层级掌握度表。 |
| `docs/superpowers/plans/`                                      | 历史规划文档目录（聊天、图谱构建、图谱可视化的设计记录）。                                                    |
| `docs/superpowers/specs/`                                      | 历史设计说明目录。                                                                           |

### 5.3 `frontend/`

#### 5.3.1 根级配置文件

| 路径                            | 作用                                                             |
| ----------------------------- | -------------------------------------------------------------- |
| `frontend/package.json`       | 前端依赖和脚本定义文件。声明了 Vue、Element Plus、ECharts、3d-force-graph、Tailwind CSS、lucide-vue-next 等。 |
| `frontend/package-lock.json`  | npm 锁文件，固定依赖版本。                                                |
| `frontend/index.html`         | Vite 前端入口 HTML。                                                |
| `frontend/tsconfig.json`      | TypeScript 编译配置。                                               |
| `frontend/tsconfig.node.json` | Node / Vite 相关的 TypeScript 配置。                                 |
| `frontend/vite.config.ts`     | Vite 配置：`@` 别名、开发端口 `8000`（strictPort: true）、`/api` 代理到 `http://localhost:8006`。 |

#### 5.3.2 `frontend/src/` 顶层

| 路径                     | 作用                                                 |
| ---------------------- | -------------------------------------------------- |
| `frontend/src/main.ts` | 前端入口，创建 Vue 应用，注册 Pinia、Router、Element Plus，并引入全局样式（含 Tailwind CSS）。 |
| `frontend/src/App.vue` | 根组件，只负责渲染 `<router-view />`。                       |

#### 5.3.3 `frontend/src/api/`

| 路径                               | 作用                                                                   |
| -------------------------------- | -------------------------------------------------------------------- |
| `frontend/src/api/ai.ts`         | 聊天 API。实现 Agent 模式（`sendAgentMessage`）和快速模式（`sendQuickMessage`），解析 SSE 多事件流（tool_used / tool_result / kg_hit / content / done / error）。 |
| `frontend/src/api/analysis.ts`   | AI 学习分析 API。`fetchStuAnalysis(stuId)` 发起 ReAct Agent 多维分析。             |
| `frontend/src/api/learningPlan.ts` | 学习规划 API。`fetchLearningPlan(stuId)` 发起按学科分别制定的学习规划（timeout=120s）。 |
| `frontend/src/api/auth.ts`       | 认证 API。注册（学生/教师）、登录、获取当前用户（`fetchCurrentUser`）、更新个人信息（`updateProfile`）。  |
| `frontend/src/api/kgPipeline.ts` | 知识图谱构建 API。提交构建任务、查询任务状态、查询结果。                                     |
| `frontend/src/api/knowledge.ts`  | 图谱浏览 API。获取图谱列表、图数据（GraphData）、搜索节点、删除图谱。                           |
| `frontend/src/api/learning.ts`   | 学习管理 API。`fetchMasteryHierarchy(courseId)` 获取某学科下学生的掌握度层级树（学科→章节→小节→知识点，含 degree/process/答题统计），用于知识图谱节点掌握度百分比可视化。 |
| `frontend/src/api/ocr.ts`        | OCR API。提交 OCR 任务、查询状态。                                              |
| `frontend/src/api/practice.ts`   | 题目练习 API。获取学科列表、题目列表、题目详情、提交答案、Dashboard 新题/巩固题推荐。                    |
| `frontend/src/api/teacher.ts`    | 教师端 API。获取当前登录教师所授学科列表（`/teacher/courses`）、所管班级列表含学生数量（`/teacher/classes`）、指定班级学生列表含学科进度与评级（`/teacher/classes/{id}/students`）、疑难知识点词云（`/teacher/classes/{id}/difficult-knowledge`）、疑难章节分布（`/teacher/classes/{id}/difficult-chapters`）。 |

#### 5.3.4 `frontend/src/components/`

| 路径                                             | 作用                                             |
| ---------------------------------------------- | ---------------------------------------------- |
| `frontend/src/components/ChatInput.vue`        | 聊天输入框，支持 Enter 发送、Shift+Enter 换行。              |
| `frontend/src/components/ChatMessage.vue`      | 聊天气泡组件。渲染用户消息、AI 消息、思考过程（可折叠）、Markdown（marked）+ 代码高亮（highlight.js）+ 数学公式（KaTeX）。 |
| `frontend/src/components/ChatSubgraphPanel.vue` | 聊天关联子图侧边面板。展示 Agent 命中知识点后的 BFS 子图。            |
| `frontend/src/components/SubgraphChart.vue`     | 子图 ECharts 渲染组件。展示命中节点（高亮）+ 上下游节点 + 边，支持节点点击跳转图谱页。 |
| `frontend/src/components/KnowledgeGraph2D.vue`  | ECharts 2D 力导向图组件。渲染章节层和子节层的图谱，支持钻取、搜索、高亮、缩放、重置。节点为普通圆形，节点中央显示掌握度百分比数字。 |
| `frontend/src/components/KnowledgeGraph3D.vue`  | 3D 力导向图组件（3d-force-graph + Three.js）。渲染知识点层立体球面图，含粒子流连线。节点球为普通实体球体（`THREE.MeshPhongMaterial`），球体中央用 SpriteText 显示掌握度百分比数字。 |
| `frontend/src/components/GraphDetailPanel.vue`  | 图谱节点详情侧边面板，显示节点类型、关系、属性。                       |
| `frontend/src/components/BorderGlow.vue`        | 毛玻璃边框发光容器组件。页面级外层包裹，提供统一的玻璃拟态视觉风格。             |
| `frontend/src/components/FloatingLines.vue`     | 动态背景特效组件。三套布局的背景粒子/线条动画。                      |
| `frontend/src/components/GooeyNav.vue`          | 黏性导航组件（备选导航方案）。                                |
| `frontend/src/components/LetterGlitch.vue`      | 文字故障特效组件（备选动效）。                                |
| `frontend/src/components/StarBorder.vue`        | 星形边框装饰组件（备选装饰）。                                |
| `frontend/src/components/AppSidebar.vue`        | 侧边栏组件（旧版布局中使用）。                                |
| `frontend/src/components/PagePlaceholder.vue`   | 页面占位符通用组件（用于未实现页面的提示展示）。                       |

#### 5.3.5 `frontend/src/composables/`

| 路径                                          | 作用                                                   |
| ------------------------------------------- | ---------------------------------------------------- |
| `frontend/src/composables/useAuth.ts`       | 登录注册的调用封装。                                           |
| `frontend/src/composables/useChat.ts`       | 聊天核心逻辑。负责消息列表、模式切换、流式拼接、工具调用事件处理、`KgHitNode` 类型定义。 |
| `frontend/src/composables/useGraph.ts`      | 图谱页交互逻辑。搜索、聚焦节点、重置视图。                                |
| `frontend/src/composables/useKgPipeline.ts` | 图谱构建任务逻辑。提交任务、轮询状态、移除任务。                             |
| `frontend/src/composables/useOcr.ts`        | OCR 任务逻辑。提交任务、轮询状态、任务清理。                              |
| `frontend/src/composables/useSubgraph.ts`   | **子图提取逻辑**。BFS 双向搜索（最多 2 跳、每跳最多 8 节点），按度排序选重要节点，区分 upstream/downstream/both 关系方向。 |

#### 5.3.6 `frontend/src/layouts/`

| 路径                                       | 作用                               |
| ---------------------------------------- | -------------------------------- |
| `frontend/src/layouts/LayoutStudent.vue` | **学生端布局**（当前主用）。Magic Bento 悬浮控制面板（8 入口：仪表盘、知识图谱、题目练习、做题记录、AI 助学、学习计划、互动专区、消息提醒），含 FloatingLines 背景 + 退出登录按钮。 |
| `frontend/src/layouts/LayoutTeacher.vue` | **教师端布局**。Bento 面板（4 入口：班级学情仪表盘、AI 聊天、互动专区、学科管理）。 |
| `frontend/src/layouts/LayoutAdmin.vue`   | **管理员布局**。Bento 面板（4 入口：文档解析、图谱构建、学生管理、预警面板）。    |
| `frontend/src/layouts/DefaultLayout.vue` | 旧版学生端布局（已被 LayoutStudent 取代，保留兼容）。       |
| `frontend/src/layouts/TeacherLayout.vue` | 旧版教师端布局（已被 LayoutTeacher 取代，保留兼容）。        |

#### 5.3.7 `frontend/src/pages/auth/`

| 路径                                     | 作用                                         |
| -------------------------------------- | ------------------------------------------ |
| `frontend/src/pages/auth/Login.vue`    | 登录页。支持学生/教师身份切换、邮箱密码登录、注册跳转。               |
| `frontend/src/pages/auth/Register.vue` | 注册页。支持学生/教师注册（姓名、邮箱、密码、性别、评级）、登录跳转。       |

#### 5.3.8 `frontend/src/pages/student/`

| 路径                                                | 作用                                                |
| ------------------------------------------------- | ------------------------------------------------- |
| `frontend/src/pages/student/Dashboard.vue`        | **学习仪表盘**（成熟页面之一）。个人信息卡片 + AI 学习分析面板（ReAct Agent）+ 学科四宫格（进度跑道 + 新题/巩固题推荐）。 |
| `frontend/src/pages/student/Chat.vue`             | **AI 助教聊天页**。Agent/快速双模式切换、消息列表、工具调用过程展示。         |
| `frontend/src/pages/student/KnowledgeExplore.vue` | **知识图谱浏览页**。图谱选择 → 层次化钻取（2D 章→子节→3D 知识点）+ 节点搜索 + 详情面板 + 删除。支持从仪表盘按学科跳转（`?module=学科ID`，通过 `kg_graphs.course_id` 自动选中该学科绑定的图谱）。加载图谱时调用 `fetchMasteryHierarchy` 获取该学科掌握度并注入节点，实现节点掌握度百分比可视化。 |
| `frontend/src/pages/student/KgPipeline.vue`       | 知识图谱构建页。上传文档（可选绑定所属学科）、查看构建任务进度、移除任务。       |
| `frontend/src/pages/student/OcrParse.vue`         | OCR 解析页。上传文档图片、配置解析参数、查看任务状态、下载结果。                 |
| `frontend/src/pages/student/PracticeHome.vue`     | **题目练习入口页**。显示可用学科模块（从数据库课程表动态加载），点击进入练习面板。        |
| `frontend/src/pages/student/Practice.vue`         | **题目练习面板**。题目描述渲染（Markdown）、选项选择、答案提交、结果反馈；基于 Vue Router 动态原路返回。 |
| `frontend/src/pages/student/ExerciseRecords.vue`  | 做题记录页。记录浏览和搜索（功能有限）。                              |
| `frontend/src/pages/student/SubjectRecords.vue`   | 学科维度的做题记录页。按学科筛选记录。                               |
| `frontend/src/pages/student/WrongBook.vue`        | 错题本页占位。                                          |
| `frontend/src/pages/student/InteractiveZone.vue`  | **互动专区**。学生问题/对话列表（表格展示）+ 分页 + 双击进入详情。            |
| `frontend/src/pages/student/InteractiveDetail.vue` | **互动详情页**。展示单个互动帖的详细内容和回复。                       |
| `frontend/src/pages/student/MessageNotifications.vue` | **消息提醒页**。展示消息列表，与 LayoutStudent 的红点徽标联动。          |
| `frontend/src/pages/student/LearningPlan.vue`     | **学习规划页**。按学科展示 AI 生成的学习规划（总体目标/薄弱知识点/每周计划/优先突破点/老师意见补充），含维度不足与数据库异常兜底提示。 |

#### 5.3.9 `frontend/src/pages/teacher/`

| 路径                                               | 作用                                    |
| ------------------------------------------------ | ------------------------------------- |
| `frontend/src/pages/teacher/Analytics.vue`       | 班级学情仪表盘。班级/学科下拉框按登录教师身份从数据库动态读取（各占一半），学生数量动态显示；学生进度与评级列表按所选班级+学科动态展示（进度取自 `course_process`，评级取自 `stu_level`）；疑难知识点词云按所选班级+学科错题知识点占比动态展示（中间大四周小、多色）；疑难章节分布图按所选班级+学科错题知识点归类到知识图谱顶层章节后以 ECharts 环形饼图展示；AI 评级/平均分/掌握度仍为静态占位。 |
| `frontend/src/pages/teacher/Chat.vue`            | 教师端 AI 聊天页。                          |
| `frontend/src/pages/teacher/InteractiveZone.vue` | 教师端互动专区。                             |
| `frontend/src/pages/teacher/SubjectManage.vue`   | 学科管理页。                               |
| `frontend/src/pages/teacher/TeacherDashboard.vue` | 教师主仪表盘（备选）。                          |
| `frontend/src/pages/teacher/AiClassAssistant.vue` | AI 课堂助手占位。                           |
| `frontend/src/pages/teacher/AssignmentManage.vue` | 作业管理占位。                              |
| `frontend/src/pages/teacher/KnowledgeGraphManage.vue` | 图谱管理占位。                              |
| `frontend/src/pages/teacher/LessonPlan.vue`      | 教案推荐占位。                              |
| `frontend/src/pages/teacher/AssignmentGrade.vue` | 作业批改页占位。                             |
| `frontend/src/pages/teacher/Alert.vue`           | 预警页占位。                               |
| `frontend/src/pages/teacher/CourseManage.vue`    | 课程管理页占位。                             |
| `frontend/src/pages/teacher/StudentManage.vue`   | 学生管理页占位。                             |

#### 5.3.10 `frontend/src/pages/misc/`

| 路径                           | 作用      |
| ---------------------------- | ------- |
| `frontend/src/pages/misc/404.vue` | 404 页面。 |

#### 5.3.11 `frontend/src/router/`

| 路径                             | 作用                                                                                                                                                                       |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `frontend/src/router/index.ts` | 定义全站路由。学生端 12 个子路由，教师端 4 个子路由，含公开路由（login/register/404）、角色路由元信息（meta.role）、通配 404 捕获。                                                                             |
| `frontend/src/router/guards.ts` | **路由守卫**。`beforeEach` 拦截逻辑：公开路由直接放行 → 检查 token 存在性 → 解析用户角色 → 角色与路由 requiredRole 匹配验证 → admin 角色全通 → 不匹配重定向到自己的 dashboard。 |

#### 5.3.12 `frontend/src/stores/` + `frontend/src/store/`

| 路径                                 | 作用                                          |
| ---------------------------------- | ------------------------------------------- |
| `frontend/src/stores/auth.ts`      | 登录状态 store。维护 token（内存 + localStorage 双写）、user（内存），提供 setToken/setUser/logout。 |
| `frontend/src/stores/knowledge.ts` | 图谱状态 store。图谱列表、当前图谱选择、图数据加载、节点选择/搜索/删除。      |
| `frontend/src/stores/learning.ts`  | 学习状态 store。progress 和 plan 预留字段。              |
| `frontend/src/store/messages.ts`   | **消息 store**。维护 30 条模拟消息 + 未读数量计算（`unreadCount`），供 LayoutStudent 红点徽标联动。 |

#### 5.3.13 `frontend/src/utils/` + `frontend/src/lib/` + `frontend/src/constants/`

| 路径                                     | 作用                             |
| -------------------------------------- | ------------------------------ |
| `frontend/src/utils/request.ts`        | Axios 实例封装。统一注入 JWT token、401 拦截跳转 login。 |
| `frontend/src/utils/kgHierarchy.ts`    | **图谱层次解析工具**。`parseHierarchy()` 解析章节父子关系；`getVisibleData()` 按钻取路径过滤可视数据；`tryDrillInto()` 判断节点是否触发钻取。 |
| `frontend/src/lib/utils.ts`            | **Tailwind 类名合并工具**。`cn()` = `twMerge(clsx(...))`，用于组件中动态合并 CSS 类名。 |
| `frontend/src/constants/knowledgeColors.ts` | **知识图谱节点类型颜色映射**。Concept → 紫色、Algorithm → 琥珀色、DataStructure → 翠绿色、Protocol → 蓝色、Principle → 粉色、Term → 灰色、Technology → 青色、Model → 橙色。命中节点高亮色：橙色。 |

### 5.4 `backend/`

#### 5.4.1 根级文件

| 路径                         | 作用                                             |
| -------------------------- | ---------------------------------------------- |
| `backend/Dockerfile`       | 后端镜像构建文件，基于 `python:3.11-slim`，用 `uvicorn` 启动。 |
| `backend/requirements.txt` | 后端 Python 依赖清单。                                |
| `backend/alembic.ini`      | Alembic 配置文件。                                  |

#### 5.4.2 `backend/alembic/versions/`

| 路径                                                    | 作用                                                     |
| ----------------------------------------------------- | ------------------------------------------------------ |
| `backend/alembic/versions/001_add_kg_graphs_table.py` | 创建 `kg_graphs` 表（图谱元数据）。                                |
| `backend/alembic/versions/002_student_side_tables.py` | 创建 students、teachers、courses、questions、exercise_records、student_course_mastery、student_knowledge_mastery、teacher_student 共 8 张表。 |
| `backend/alembic/versions/003_add_document_chunks.py` | 创建 `document_chunks` 表（含 `embedding vector(768)` 列 + HNSW 索引，用于 pgvector 文档检索）。 |
| `docs/sql/002_alter_tables.sql` | 增量 SQL 脚本。为 `kg_graphs` 表新增 `course_id` 列（学科 → 知识图谱 映射），并建索引。 |

#### 5.4.3 `backend/app/`

| 路径                        | 作用                                                                                                                                                                    |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `backend/app/__init__.py` | Python 包标记文件。                                                                                                                                                         |
| `backend/app/main.py`     | 后端入口。注册 auth、knowledge、learning、practice、teacher、ai_gateway、ocr_gateway、kg_gateway 共 8 个路由模块，启用 CORS。                                                               |

#### 5.4.4 `backend/app/api/`

| 路径                               | 作用                                                                 |
| -------------------------------- | ------------------------------------------------------------------ |
| `backend/app/api/auth.py`        | **认证路由**（已实现）。学生/教师注册、登录（区分 user_type）、获取当前用户（`/me`）、更新个人信息（`/profile`）。 |
| `backend/app/api/ai_gateway.py`  | AI 聊天网关。转发 Agent 模式和快速模式请求，SSE 透传。                                |
| `backend/app/api/kg_gateway.py`  | 知识图谱网关。创建构建任务（可绑定 `course_id` 学科）、查状态、查结果、查图数据、搜节点、删图谱。当前非常关键。                         |
| `backend/app/api/knowledge.py`   | 图谱元数据接口。列出当前用户图谱、删除图谱。                                              |
| `backend/app/api/ocr_gateway.py` | OCR 网关。接收多文件表单并转发到 AI OCR 服务。                                      |
| `backend/app/api/practice.py`    | **题目练习路由**（已实现）。学科列表、题目列表、题目详情、提交答案、Dashboard 推荐题目。                  |
| `backend/app/api/learning.py`    | 学习管理接口。`GET /learning/mastery/hierarchy?course_id=X` 返回某学科下学生的掌握度层级树（学科→章节→小节→知识点），由 `MasteryService` 聚合生成。 |
| `backend/app/api/teacher.py`     | 教师管理接口。`/courses` 获取当前登录教师所授学科、`/classes` 获取所管班级（含学生数量）、`/classes/{id}/students` 获取班级学生（含学科进度与评级）、`/classes/{id}/difficult-knowledge` 获取疑难知识点词云、`/classes/{id}/difficult-chapters` 获取疑难章节分布；其余（学情分析/预警/作业）仍为占位。 |

#### 5.4.5 `backend/app/core/`

| 路径                             | 作用                                         |
| ------------------------------ | ------------------------------------------ |
| `backend/app/core/config.py`   | 后端配置中心。读取数据库、Redis、AI 地址、AGE、JWT、模型、pgvector 等配置。 |
| `backend/app/core/database.py` | 异步数据库连接和会话管理。                              |
| `backend/app/core/deps.py`     | 依赖注入。`get_current_user`（严格认证）、`get_current_user_optional`（可选匿名，id=1 兜底）。 |
| `backend/app/core/security.py` | 密码哈希（passlib + bcrypt）、密码校验、JWT 创建和解析。     |

#### 5.4.6 `backend/app/models/`

| 路径                                    | 作用                                                             |
| ------------------------------------- | -------------------------------------------------------------- |
| `backend/app/models/kg_graph.py`      | `kg_graphs` 表 ORM 模型（含 `course_id` 学科绑定）。                                        |
| `backend/app/models/user.py`          | `students` 和 `teachers` 表 ORM 模型（两个 class：`Student` + `Teacher`）。 |
| `backend/app/models/course.py`        | `courses` 表 ORM 模型（关联 `kg_graphs`）。                          |
| `backend/app/models/question.py`      | `questions` 表 ORM 模型（含 options JSON 列、难度约束、课程外键）。            |
| `backend/app/models/exercise_record.py` | `exercise_records` 表 ORM 模型（做题记录，关联 question/student/kg/course）。   |
| `backend/app/models/knowledge.py`     | 知识点 ORM 占位。                                                     |
| `backend/app/models/learning.py`      | 学习记录 ORM。`student_course_mastery`（含 `course_degree` 掌握度、`course_process` 进度）、`student_knowledge_mastery`（知识点掌握度，含 `course_id` 学科维度、`answered_count`/`correct_count` 答题统计，主键 `(stu_id, course_id, kg_node_name)`），以及 `student_section_mastery`、`student_chapter_mastery`、`student_course_mastery` 三张层级掌握度表。 |
| `backend/app/models/teacher.py`       | 教师/班级 ORM 占位。                                                   |
| `backend/app/models/teacher_relation.py` | `teacher_course`（教师-学科）与 `teacher_class`（教师-班级）关联表 ORM 模型。 |

#### 5.4.7 `backend/app/schemas/`

| 路径                                 | 作用                                                      |
| ---------------------------------- | ------------------------------------------------------- |
| `backend/app/schemas/auth.py`      | **认证 Pydantic 模型**（已实现）。StudentRegisterRequest、TeacherRegisterRequest、LoginRequest、TokenResponse、StudentResponse、TeacherResponse、StudentUpdateRequest。 |
| `backend/app/schemas/kg_graph.py`  | 图谱元数据 Pydantic 模型。                                      |
| `backend/app/schemas/course.py`    | **课程 Pydantic 模型**（已实现）。CourseCreate、CourseUpdate、CourseResponse。 |
| `backend/app/schemas/practice.py`  | **题目练习 Pydantic 模型**（已实现）。QuestionCreate、QuestionResponse、ExerciseRecordCreate、ExerciseRecordResponse。 |
| `backend/app/schemas/knowledge.py` | 知识图谱请求/响应模型占位。                                          |
| `backend/app/schemas/learning.py`  | 学习管理模型。`StudentKnowledgeMasteryCreate/Response` 含 `course_id`、`answered_count`、`correct_count` 字段。 |
| `backend/app/schemas/teacher.py`   | 教师管理模型占位。                                               |

#### 5.4.8 `backend/app/services/`

| 路径                                          | 作用                                                          |
| ------------------------------------------- | ----------------------------------------------------------- |
| `backend/app/services/auth_service.py`      | **认证业务逻辑**（已实现）。注册（密码哈希）、登录（密码验证 + JWT 签发）、获取用户信息、更新个人信息。  |
| `backend/app/services/kg_graph_service.py`  | 图谱元数据服务。创建（可绑定 `course_id`）、查询、更新、删除图谱记录。                                    |
| `backend/app/services/course_service.py`    | **课程业务逻辑**（已实现）。CRUD 操作。                                     |
| `backend/app/services/practice_service.py`  | **题目练习业务逻辑**（已实现）。题目查询、做题提交（按题型判分：客观题判 do_isTrue、Fill_blanks 按空给分、Q_A 大模型评分；kg_id/kg_node_name 从题库继承；iserror_firstly 历史首次判定；记录持久化 + 掌握度更新）、Dashboard 推荐题目。  |
| `backend/app/services/knowledge_service.py` | 知识图谱业务逻辑占位。                                                 |
| `backend/app/services/learning_service.py`  | 学习管理业务逻辑占位。                                                 |
| `backend/app/services/mastery_service.py`   | **学生知识图谱掌握度服务**（已实现）。`update_knowledge_mastery()` 做题后更新知识点掌握度（客观题 5/0 分、主观题按得分映射，加权平均）；`get_mastery_hierarchy()` 从 AGE 图解析章节层级并聚合出学科→章节→小节→知识点的掌握度树（知识点平均→小节→章节→学科）。 |
| `backend/app/services/teacher_service.py`   | 教师业务逻辑。通过 `teacher_course` 关联表获取所授学科、通过 `teacher_class` 关联表获取所管班级并统计学生数量；`get_class_students` 校验教师-班级归属后返回班级学生（含所选学科进度 `course_process` 与评级 `stu_level`）；`get_difficult_knowledge_points` 统计该班级该学科错题知识点占比并取前 20 生成词云；`get_difficult_chapters` 统计该班级该学科错题知识点并归类到知识图谱顶层章节（章节→小节→知识点，小节无知识点时视为知识点本身）生成疑难章节分布。 |

### 5.5 `ai/`

#### 5.5.1 根级文件

| 路径                    | 作用                                                              |
| --------------------- | --------------------------------------------------------------- |
| `ai/Dockerfile`       | AI 服务镜像构建文件。                                                    |
| `ai/requirements.txt` | AI 服务依赖清单。包含 FastAPI、Redis、APScheduler、mineru、torch、chromadb、mcp、psycopg2、pgvector 等。 |

#### 5.5.2 `ai/scripts/`

| 路径                        | 作用                                              |
| ------------------------- | ----------------------------------------------- |
| `ai/scripts/build_kg.py`  | 离线构建知识图谱脚本骨架。目前只解析命令行参数。                        |
| `ai/scripts/train_gnn.py` | 离线训练 GNN 脚本骨架。目前只解析命令行参数。                        |

#### 5.5.3 `ai/app/`

| 路径                       | 作用                                                                                                                                                                               |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ai/app/config.py`       | AI 服务配置中心。读取模型地址、Redis、OCR、AGE、KG 抽取模型、MCP 数据库 URL、pgvector 等配置。                                                                                                                  |
| `ai/app/dependencies.py` | AI 服务公共依赖。`verify_service_token` 校验 `X-Service-Token` 请求头。                                                                                                                         |
| `ai/app/main.py`         | AI 服务入口。lifespan 中：清理 Redis 残留缓存 → 启动 scheduler → 启动 OCR task manager → 启动 Redis worker → 连接 MCP client。注册 chat、gnn、kg、analysis 路由 + OCR 路由。 |

#### 5.5.4 `ai/app/api/`

| 路径                         | 作用                                                              |
| -------------------------- | --------------------------------------------------------------- |
| `ai/app/api/chat.py`       | 聊天接口。Agent 模式（`/rag/chat/agent`，ReAct 循环 + SSE 多事件流）、快速模式（`/rag/chat/quick`，直接流式输出）、深度思考模式。 |
| `ai/app/api/analysis.py`   | **AI 学习分析接口**。`POST /analysis/stu_analysis` → ReAct Agent 三维分析 → 返回结构化报告。  |
| `ai/app/api/learning_plan.py` | **学习规划接口**。`POST /analysis/learning_plan` → ReAct Agent 按学科分别制定规划（四维度等权 + 兜底机制）。 |
| `ai/app/api/qa_score.py`    | **简答题（Q_A）评分接口**。`POST /analysis/qa_score` → 大模型根据题目题干/标准答案对比学生回答酌情给分（满分 10 分，float）。 |
| `ai/app/api/kg.py`         | 知识图谱构建与查询接口。建任务、查状态、查结果、查全图、搜索节点、删除图。                             |
| `ai/app/api/gnn.py`        | GNN 推荐接口骨架。                                                      |

#### 5.5.5 `ai/app/agent/`（Agent 框架 — 核心新增模块）

| 路径                                  | 作用                                                              |
| ----------------------------------- | --------------------------------------------------------------- |
| `ai/app/agent/__init__.py`          | Agent 模块入口。"具 Tool-Calling 能力的智能对话引擎"。                           |
| `ai/app/agent/orchestrator.py`      | **Agent 编排器**（核心）。ReAct 循环（最多 10 轮）：LLM 决策 → 工具分发 → 结果注入 → 继续循环 → 流式输出。含动态系统提示词生成、kg_hit 事件发射、超时控制。 |
| `ai/app/agent/tool_registry.py`     | **工具注册中心**。统一管理本地工具（装饰器注册）和 MCP 远程工具。提供 `get_definitions()`（OpenAI function-calling 格式）、`execute()`（工具分发执行）、结果截断。 |
| `ai/app/agent/mcp_client.py`        | **MCP 客户端**。连接到 MCP Server（如 `mcp_db_server.py`），获取远程工具列表并注册到 ToolRegistry。 |
| `ai/app/agent/schemas.py`           | Agent 数据模型。`ToolDef`（工具定义：name、description、parameters、handler、is_mcp）、`AgentMessage` 等。 |
| `ai/app/agent/context.py`           | **协程安全上下文**。通过 `contextvars` 传递 `current_kg_graph_ids` 和 `current_graph_names`，供 Agent 系统提示词和工具使用。 |
| `ai/app/agent/router.py`            | Agent 路由（备选入口）。                                                |
| `ai/app/agent/stu_analysis_agent.py` | **学生分析 Agent**。编排三维度数据收集 → 综合报告生成。                              |
| `ai/app/agent/learning_plan_agent.py` | **学习规划 Agent**。按学科分别制定规划：ReAct 循环收集四维度数据 → 确定性计算权重（等权 1/N）→ 兜底判定（db_error / insufficient / ok）→ 权重约束提示词生成细粒度规划。 |
| `ai/app/agent/qa_score_agent.py` | **简答题评分 Agent**。`QaScoreAgent.score()` → 大模型根据题目题干/标准答案对比学生回答酌情给分（满分 10 分，float），含 JSON 解析容错。 |
| `ai/app/agent/tools/__init__.py`    | 工具包入口。                                                          |
| `ai/app/agent/tools/search_kg.py`   | **知识图谱查询工具**。`@ToolRegistry.register("search_kg")` → 调用 `kg_pipeline/queries.py` 搜索节点。 |
| `ai/app/agent/tools/read_document.py` | **文档检索工具**。`@ToolRegistry.register("read_document")` → 调用 `engines/rag/pipeline.py`（pgvector 语义检索）。 |
| `ai/app/agent/tools/stu_analysis_db.py` | **学生分析数据库工具**。三个查询函数：`query_student_level`、`query_knowledge_mastery`、`query_wrong_exercises`。返回统一结构 `{success, data, summary}`。 |
| `ai/app/agent/tools/learning_plan_db.py` | **学习规划数据库工具**。五个查询函数：`query_subjects`、`query_ai_analysis`、`query_teacher_opinion`、`query_knowledge_mastery`、`query_exercise_progress`（题库题目总数 + 已做题目数 → 各科进度）。返回统一结构 `{success, error_type, data, summary}`，`error_type` 区分 `db_error`/`no_data`。 |

#### 5.5.6 `ai/app/engines/`

| 路径                           | 作用             |
| ---------------------------- | -------------- |
| `ai/app/engines/__init__.py` | engines 包标记文件。 |

##### `ai/app/engines/llm/`

| 路径                               | 作用                                                                            |
| -------------------------------- | ----------------------------------------------------------------------------- |
| `ai/app/engines/llm/client.py`   | **AI 核心之一**。统一模型调用入口。支持 `chat()`（含 tool_calls 解析）和 `stream()`（SSE 逐 token 输出）。含重试、超时控制。 |
| `ai/app/engines/llm/profiles.py` | 模型端点配置。`quick_profile`（Qwen3.5-9B）、`remote_profile`、`deepseek_profile`、`local_profile`。 |

##### `ai/app/engines/graphrag/`

| 路径                                     | 作用                             |
| -------------------------------------- | ------------------------------ |
| `ai/app/engines/graphrag/retriever.py` | RAG 检索器骨架。                     |
| `ai/app/engines/graphrag/generator.py` | RAG 生成器骨架。                     |
| `ai/app/engines/graphrag/prompts.py`   | RAG 提示词模板。                     |

##### `ai/app/engines/gnn/`

| 路径                                | 作用         |
| --------------------------------- | ---------- |
| `ai/app/engines/gnn/model.py`     | GNN 模型骨架。  |
| `ai/app/engines/gnn/trainer.py`   | GNN 训练骨架。  |
| `ai/app/engines/gnn/inference.py` | GNN 推理骨架。  |

##### `ai/app/engines/rag/`（新增）

| 路径                              | 作用                    |
| ------------------------------- | --------------------- |
| `ai/app/engines/rag/pipeline.py` | RAG 流水线（文档检索 + 生成）。 |

#### 5.5.7 `ai/app/kg_pipeline/`

| 路径                                             | 作用                                                             |
| ---------------------------------------------- | -------------------------------------------------------------- |
| `ai/app/kg_pipeline/__init__.py`               | KG pipeline 模块对外导出入口。                                          |
| `ai/app/kg_pipeline/chunking.py`               | Markdown 切片器。按标题、段落、句子切块。                                      |
| `ai/app/kg_pipeline/extraction.py`             | 知识图谱抽取器。调用 LLM 从文本块里抽实体和关系（少量样本提示词 + JSON 结构化输出）。               |
| `ai/app/kg_pipeline/graph_builder.py`          | 图合并器。把多个局部图合并成 NetworkX 图，并做节点去重（同名+同类型→合并属性）与边验证。                |
| `ai/app/kg_pipeline/models.py`                 | KG pipeline 数据模型：节点、边、切片、结果等。                                  |
| `ai/app/kg_pipeline/pipeline.py`               | **图谱构建总流程**。`run_from_file()`：OCR → 切片 → 抽取 → 合图 → 写入 AGE。        |
| `ai/app/kg_pipeline/storage.py`                | **AGE 存储适配层**。`create_graph()`、`write_graph()`（批量 INSERT VERTEX/EDGE）、`drop_graph()`。 |
| `ai/app/kg_pipeline/queries.py`                | **AGE 查询层**。`get_full_graph()`（MATCH ... RETURN 全图）、`search_nodes()`（按名称/类型匹配）。 |
| `ai/app/kg_pipeline/prompts/kg_extraction.txt` | KG 抽取系统提示词。规范实体类型（Concept/Algorithm/DataStructure/Protocol/Principle/Term/Technology/Model/Chapter）、关系命名（包含/前置依赖/应用/属于/相关）和 JSON 输出格式。 |

#### 5.5.8 `ai/app/ocr/`

| 路径                           | 作用                                       |
| ---------------------------- | ---------------------------------------- |
| `ai/app/ocr/router.py`       | OCR API。提交任务、查状态、下载结果。                   |
| `ai/app/ocr/schemas.py`      | OCR 任务和参数数据模型。                           |
| `ai/app/ocr/service.py`      | OCR 服务层。保存上传文件、调用 mineru（`aio_do_parse`）、打包结果 ZIP。 |
| `ai/app/ocr/task_manager.py` | OCR 异步任务管理器。队列、并发控制（信号量）、状态管理、过期清理。        |

#### 5.5.9 `ai/app/schemas/`

| 路径                           | 作用             |
| ---------------------------- | -------------- |
| `ai/app/schemas/chat.py`     | 聊天请求/响应模型。     |
| `ai/app/schemas/common.py`   | 通用任务模型。        |
| `ai/app/schemas/gnn.py`      | GNN 请求/响应模型。   |

#### 5.5.10 `ai/app/tasks/`

| 路径                               | 作用                                            |
| -------------------------------- | --------------------------------------------- |
| `ai/app/tasks/registry.py`       | **任务注册中心**。`@task_handler` 和 `@scheduled_task` 装饰器自注册。 |
| `ai/app/tasks/worker.py`         | **Redis 队列消费者**。BRPOP 阻塞获取任务 → 调用对应 handler。   |
| `ai/app/tasks/scheduler.py`      | APScheduler 调度器封装。                            |
| `ai/app/tasks/kg_build.py`       | **知识图谱构建任务处理器**（真实在用）。                        |
| `ai/app/tasks/kg_extract.py`     | 知识图谱抽取任务骨架。                                   |
| `ai/app/tasks/daily_profile.py`  | 每日画像任务骨架（已注册调度，逻辑未实现）。                        |
| `ai/app/tasks/daily_question.py` | 每日一题任务骨架（已注册调度，逻辑未实现）。                        |

### 5.6 `scripts/`

| 路径                          | 作用                                                                           |
| --------------------------- | ---------------------------------------------------------------------------- |
| `scripts/mcp_db_server.py`  | **MCP 数据库服务**（端口 8005）。基于 FastMCP + asyncpg，暴露 `list_tables`、`get_table_schema`、`execute_readonly_sql` 三个工具。防 SQL 注入（关键字过滤 + 只读限制 + 50 行限制）。 |
| `scripts/test_deploy.py`    | 模型部署测试脚本。用 `transformers + torch` 直接加载模型路径验证推理环境。                               |

### 5.7 `MyideaAndIknow/`

| 路径                                             | 作用                                                   |
| ---------------------------------------------- | ---------------------------------------------------- |
| `MyideaAndIknow/mcp_database_architecture_plan.md` | **双检索架构设计文档**。讨论 GraphRAG（模糊语义查询）和 MCP + 数据库（精准结构化查询）双链路的架构设计、安全考量和实现思路。 |

### 5.8 第三方依赖目录

| 路径                       | 说明               |
| ------------------------ | ---------------- |
| `backend/venv/`          | 后端 Python 虚拟环境。  |
| `ai/venv/`               | AI 服务 Python 虚拟环境。 |
| `frontend/node_modules/` | 前端 npm 依赖目录。    |

***

## 六、项目实际运行架构

### 6.1 当前代码里能看出来的运行口径

#### 本地开发口径（本文档使用）

| 服务         | 端口   | 说明                          |
| ---------- | ---- | --------------------------- |
| 前端 Vite    | 8000 | `strictPort: true`，开发服务器    |
| 后端 FastAPI | 8006 | Vite 代理 `/api → http://localhost:8006` |
| AI FastAPI | 8009 | 本地开发端口                      |
| MCP DB 服务  | 8005 | 数据库工具服务                     |
| 聊天模型服务     | 8002 | vLLM + Qwen3.5-9B           |
| OCR 模型服务   | 8004 | vLLM + MinerU2.5-Pro        |

#### 远程服务器口径（CLAUDE.md 描述）

| 服务         | 端口   |
| ---------- | ---- |
| 前端 Vite    | 8000 |
| 后端 FastAPI | 8006 |
| AI FastAPI | 8001 |
| MCP DB 服务  | 8005 |
| 聊天模型服务     | 8002 |
| OCR 模型服务   | 8004 |

### 6.2 端口对照与代理链路

```
浏览器访问 http://localhost:8000/student/chat
  → Vite (8000) 提供静态资源
  → 前端 fetch('/api/ai/chat/agent', ...)
  → Vite proxy 转发到 http://localhost:8006/api/ai/chat/agent
  → Backend (8006) ai_gateway.py 接收请求
  → Backend 通过 httpx 转发到 AI_SERVICE_URL（本地 http://localhost:8009）
  → AI (8009) Agent 处理
  → 如需 LLM → AI 通过 httpx 调用 vLLM (8002)
  → 如需数据库查询 → Agent 通过 MCP client 连接 MCP Server (8005) → PostgreSQL
  → SSE 流逐级返回：AI → Backend → 前端
```

### 6.3 Docker 现状

| 文件                              | 现状                                     |
| ------------------------------- | -------------------------------------- |
| `docker-compose.middleware.yml` | 相对可信，可用于启动 postgres / redis / chromadb |
| `docker-compose.yml`            | 不完全可直接用，原因是 `frontend/Dockerfile` 缺失   |

### 6.4 图谱功能的额外环境要求

知识图谱功能不仅需要 PostgreSQL + Redis + AI 服务 + OCR 模型服务 + LLM 抽取模型，还需要：

- **Apache AGE 扩展环境**。因为 `AgeStorage` 中会直接执行：

```sql
LOAD 'age';
SET search_path TO ag_catalog, public;
```

如果运行环境里没有 AGE，知识图谱主链路就跑不通。

### 6.5 Agent 功能的额外环境要求

Agent 聊天模式和学习分析功能依赖：

- **MCP 数据库服务**（`scripts/mcp_db_server.py`，端口 8005），为 Agent 提供结构化的数据库查询能力
- **pgvector 扩展**（PostgreSQL 扩展），为 `read_document` 文档检索工具提供向量相似度搜索

***

## 七、建议你如何理解当前项目

### 第一层：已经证明可行的"智能处理底座"

这层已经比较有实物感了：

1. 前端上传与任务轮询
2. 后端网关与元数据管理
3. AI 异步任务系统
4. OCR（mineru 文档解析）
5. LLM 调用（httpx 封装的 OpenAI 兼容接口）
6. Agent 多步推理（ReAct + Tool Registry + MCP）
7. 图谱构建和层次化查询（Pipeline → AGE → kgHierarchy → 2D/3D 渲染）
8. 用户认证体系（注册/登录/JWT/路由守卫）
9. 题库练习（2484 道题 + 做题提交 + 自动判分）

### 第二层：已经搭好骨架但还没填肉的业务模块

包括：

1. 做题记录浏览与归因分析（提交已实现，分析尚浅）
2. 教师端深度功能（作业批改、学生管理、预警）
3. 互动专区后端（前端 UI 已完成）
4. 定时任务（调度器可工作，业务逻辑未实现）

### 第三层：明确规划但尚未落地的高级智能能力

包括：

1. GraphRAG 混合检索（ChromaDB 多跳检索）
2. GNN 推荐（模型训练 + 推理）
3. 每日画像生成
4. 每日一题生成

***

## 八、接手建议

### 8.1 如果你要先看懂项目，建议阅读顺序

1. `frontend/src/router/index.ts` + `guards.ts`
   - 看前端有哪些页面、访问控制逻辑
2. `frontend/src/layouts/LayoutStudent.vue`
   - 看学生端整体 UI 架构（Bento 面板 + 背景特效）
3. `frontend/src/pages/student/Dashboard.vue`
   - 看最完整的页面：个人信息 + AI 分析 + 学科面板
4. `frontend/src/pages/student/Chat.vue`
   - 看 Agent 聊天功能从 UI 层怎么发出去、怎么处理事件流
5. `backend/app/api/auth.py` + `backend/app/services/auth_service.py`
   - 看认证体系完整实现
6. `backend/app/api/practice.py` + `backend/app/services/practice_service.py`
   - 看题库练习的完整链路
7. `ai/app/agent/orchestrator.py` + `ai/app/agent/tool_registry.py`
   - 看 Agent 框架核心：多步推理 + 工具调度
8. `ai/app/agent/tools/search_kg.py` + `ai/app/agent/tools/stu_analysis_db.py`
   - 看 Agent 工具的具体实现模式
9. `ai/app/kg_pipeline/pipeline.py`
   - 看知识图谱构建主链路
10. `ai/app/kg_pipeline/storage.py` + `queries.py`
    - 看 AGE 如何写入和查询
11. `frontend/src/utils/kgHierarchy.ts` + `frontend/src/composables/useSubgraph.ts`
    - 看前端图谱层次解析和子图提取逻辑

### 8.2 如果你要继续开发，优先级建议

| 优先级 | 任务                     | 原因               |
| --- | ---------------------- | ---------------- |
| P0  | 补全 AGE 安装文档或自动化脚本      | 知识图谱核心依赖        |
| P1  | 完成文档向量化入库流程           | `read_document` 工具依赖，当前仅有骨架 |
| P2  | 完善做题记录分析与归因           | 数据已入库，展示和分析不足    |
| P3  | 补全互动专区后端数据接口          | 前端 UI 已完成，后端仍 Mock |
| P4  | 补齐 frontend/Dockerfile | 让完整 Docker 部署可行   |
| P5  | 完善学习规划（已实现主链路）      | 按学科分别制定，四维度等权 + 兜底机制已落地；可进一步优化规划细粒度与交互 |
| P6  | 真正接入 GraphRAG 混合检索     | 让 Agent 具备多跳图谱检索  |
| P7  | 实现每日画像/每日一题           | 利用已有的调度框架        |
| P8  | 补全教师端功能               | 作业批改、学生管理、预警    |
| P9  | 实现 GNN 推荐              | 这是更后面的高级能力       |

***

## 九、最终判断

### 9.1 这个项目现在最准确的定位

它不是一个"已经全部完成的教学平台"，而是一个：

> **以 Agent 对话、OCR 文档解析、知识图谱构建与层次化浏览、AI 学习分析为核心原型的 AI 教学平台，已具备较为完整的"数据采集 → 智能处理 → 可视化呈现"闭环能力**

### 9.2 它已经有价值的地方

真正有价值、而且不是空壳的核心能力：

1. **三服务分层架构清晰**：前端（Vue 3 + 路由守卫）→ 后端（FastAPI + JWT + 网关）→ AI（Agent + 工具 + 任务队列）
2. **Agent 框架已成型**：ReAct 多步推理 + 本地工具装饰器自注册 + MCP 远程工具集成 + 上下文协程安全传递
3. **知识图谱构建流水线已从前端贯通到图数据库**：上传 → OCR → LLM 抽取 → NetworkX 合并 → AGE 存储 → 层次化钻取浏览
4. **用户系统已完整实现**：注册/登录/JWT/角色路由守卫/个人信息管理
5. **题库系统基础已具备**：2484 道考研题目入库、做题提交判分、Dashboard 推荐
6. **AI 学习分析是亮点**：ReAct Agent 自主收集三维度数据 → 生成综合分析报告，展示了 Agent 框架的实际应用价值
7. **学习规划已落地**：ReAct Agent 按学科分别制定规划，综合四维度（AI 分析/知识图谱/习题/老师意见）等权加权，含 db_error / insufficient 兜底机制

### 9.3 它还缺什么

它还缺的是"平台层业务"的深度完成：

1. 做题记录深度分析（归因、薄弱点定位）
2. 教师端完整功能（作业批改、学情报告、预警）
3. GraphRAG 混合检索（ChromaDB 未接入）
4. GNN 推荐

也就是说：

- **智能底座有雏形，Agent 框架是亮点**
- **平台业务正在从骨架向深度发展**

***

> **文档版本**：v3.0（按 2026-08-01 项目实际代码状态全面重写）\
> **校准日期**：2026-08-01\
> **校准原则**：以仓库实际源码、配置文件、数据库迁移和现有路由为准；不把 PRD 规划内容当作已实现功能。\
> **关键变更（v2.1 → v3.0）**：
> - 新增 Agent 框架完整文档（ReAct 编排器 + Tool Registry + MCP Client + 三大工具类）
> - 新增 AI 学习分析链路文档（ReAct Agent 三维度分析）
> - 新增 MCP 数据库服务文档（FastMCP + asyncpg）
> - 新增层次化图谱浏览逻辑（kgHierarchy.ts 章节父子关系解析）
> - 新增子图提取逻辑（useSubgraph.ts BFS 双向搜索）
> - 更新用户认证系统状态（从骨架到已完成）
> - 更新题库练习系统状态（2484 道题已入库，做题/判分已实现）
> - 更新仪表盘状态（个人信息编辑 + AI 分析面板 + 学科进度跑道）
> - 更新布局系统（DefaultLayout/TeacherLayout → LayoutStudent/LayoutTeacher/LayoutAdmin + Bento 面板）
> - 新增数据库表结构文档（9 张表 + 3 个 Alembic 迁移）
> - 新增 pgvector 配置说明（document_chunks 表）
> - 修复 AI 端口说明（本地 8009 / 远程 8001 双口径）
> - 新增 Tailwind CSS + clsx + twMerge 技术说明
> - 新增学习规划链路文档（ReAct Agent 按学科分别制定，四维度等权 + 兜底机制）
> - 更新学习计划状态（从占位到主链路已实现）
> - 更新数据库表结构文档（新增 evaluation_analysis 表，共 10 张表）
> - 更新目录与文件逐项说明（新增 30+ 文件条目）
